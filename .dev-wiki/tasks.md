# Tasks

> Last updated: 2026-05-02 by nana (agent-planned)

<details>
<summary>Phase 0: Fresh Fork + Smoke Test (7/7 done)</summary>

- [x] Fork and clone upstream NanoClaw v2.0.13 | scope: . | success: `git remote -v` shows origin=j-wanger/nanaclaw, upstream=qwibitai/nanoclaw | size: S
- [x] Initialize dev-wiki and run full codebase scan | scope: .dev-wiki/** | success: `test -f .dev-wiki/_ARCHITECTURE.md && ls .dev-wiki/articles/modules/*.md | wc -l | grep -q 8` | size: M
- [x] Build verification: test `pnpm install` + `pnpm run build` + `pnpm test` (RED), run all three and document any pre-existing failures (GREEN) | scope: package.json, pnpm-lock.yaml | success: `pnpm run build 2>&1 | tail -1 | grep -v error` | size: S
- [x] Create SOUL.md: write test checking file exists and is <50 lines (RED), create SOUL.md with agent personality (GREEN) | scope: SOUL.md | success: `test -f SOUL.md && test $(wc -l < SOUL.md) -lt 50` | size: S
- [x] Create AGENTS.md: write test checking file exists (RED), create AGENTS.md with dev conventions for TypeScript, upstream module pattern, two-DB invariants, testing (GREEN) | scope: AGENTS.md | success: `test -f AGENTS.md && grep -q 'module' AGENTS.md` | size: S
- [x] Update CLAUDE.md with fork conventions: verify current CLAUDE.md exists (RED), add fork-specific sections referencing SOUL.md, AGENTS.md, and key architectural decisions (GREEN) | scope: CLAUDE.md | success: `grep -q 'SOUL.md' CLAUDE.md && grep -q 'AGENTS.md' CLAUDE.md` | size: S
- [x] CLI smoke test: verify scripts/chat.ts exists (RED), run CLI agent setup + send test message + verify response (GREEN) | scope: scripts/chat.ts, scripts/init-cli-agent.ts | success: end-to-end message produces non-empty response (manual verification — requires Docker + OneCLI/API credentials) | size: M

</details>

<details>
<summary>Phase 1b: Memory Module Implementation (6/6 done)</summary>

- [x] Memory types and MEMORY.md store: test MemoryEntry round-trip parse/write with all 4 types, empty file, and special characters (RED), implement MemoryEntry/MemoryType types and markdown parser/writer with CRUD (GREEN), extract heading-regex constant (REFACTOR) | scope: src/modules/memory/types.ts, src/modules/memory/memory-store.ts, src/modules/memory/memory-store.test.ts | success: pnpm vitest run src/modules/memory/memory-store.test.ts | size: M
- [x] FTS5 search index: test rebuild from 100-entry fixture in <50ms and keyword search returns ranked results (RED), implement per-group SQLite FTS5 virtual table with rebuild and search functions (GREEN), add mtime-based staleness guard (REFACTOR) | scope: src/modules/memory/fts.ts, src/modules/memory/fts.test.ts | success: pnpm vitest run src/modules/memory/fts.test.ts | size: M
- [x] Context builder: test fragment from fixture MEMORY.md produces non-empty markdown under 1500-token estimate sorted by recency (RED), implement context builder that parses MEMORY.md, rebuilds FTS5, selects entries by recency within budget, writes memory-context.md to .claude-fragments (GREEN), extract token estimation utility (REFACTOR) | scope: src/modules/memory/context-builder.ts, src/modules/memory/context-builder.test.ts | success: pnpm vitest run src/modules/memory/context-builder.test.ts | size: M
- [x] Module registration and spawn integration: verify modules barrel compiles with new import (RED), create memory/index.ts exporting generateMemoryFragment, add import to modules/index.ts, call in container-runner buildMounts after composeGroupClaudeMd with try/catch (GREEN) | scope: src/modules/memory/index.ts, src/modules/index.ts, src/container-runner.ts | success: pnpm run build && pnpm vitest run | size: S
- [x] Container memory skill with cold start: verify no existing memory skill (RED), create SKILL.md and instructions.md with MEMORY.md format spec, write conventions, 3 cold-start seed questions, frozen-snapshot semantics (GREEN) | scope: container/skills/memory/SKILL.md, container/skills/memory/instructions.md | success: test -f container/skills/memory/SKILL.md && test -f container/skills/memory/instructions.md && grep -q 'seed' container/skills/memory/instructions.md && grep -q 'next spawn' container/skills/memory/instructions.md | size: S
- [x] Build and regression verification: run full build, all host tests, container typecheck, verify memory module does not reference CLAUDE.local.md [manual: end-to-end smoke test requires Docker] | scope: src/modules/memory/**, container/skills/memory/** | success: pnpm run build && pnpm vitest run && pnpm exec tsc -p container/agent-runner/tsconfig.json --noEmit && ! grep -r 'CLAUDE.local' src/modules/memory/ | size: S

</details>

<details>
<summary>Phase 2: Host-Mode Agent Runner (8/8 done)</summary>

- [x] Agent-runner path configurability: test existing agent-runner uses default container paths when no env vars set (RED), add env var overrides in config.ts and connection.ts — NANOCLAW_SESSION_DIR (replaces /workspace for DBs, heartbeat, outbox), NANOCLAW_AGENT_DIR (replaces /workspace/agent for CWD and container.json) — defaults to current hardcoded paths so container mode is byte-identical (GREEN) | scope: container/agent-runner/src/config.ts, container/agent-runner/src/db/connection.ts | success: `cd container/agent-runner && bun test` | size: S
- [x] Host provider scaffold and barrel registration: verify no host provider exists in registry (RED), create src/providers/host/index.ts with registerProviderContainerConfig('host', fn) returning empty contribution (no Docker mounts), add import './host/index.js' to src/providers/index.ts barrel (GREEN) | scope: src/providers/host/index.ts, src/providers/index.ts | success: `pnpm run build && grep -q 'host' src/providers/index.ts` | size: S
- [x] Container-runner host-mode branch and lifecycle: test resolveProviderName returns 'host' when container.json says host, verify wakeContainer does not spawn Docker for host provider (RED), add wakeHostRunner() — run pre-spawn steps (composeGroupClaudeMd, generateMemoryFragment, syncSkillSymlinks, ensureRuntimeFields), spawn bun container/agent-runner/src/index.ts as child process with env vars (NANOCLAW_SESSION_DIR, NANOCLAW_AGENT_DIR, TZ), add mode flag to activeContainers map (mode: 'host' or 'container'), wire close/error handlers, modify killContainer() to branch on mode — host: process.kill('SIGTERM'), container: stopContainer(). Verify host-sweep heartbeat checks work for native processes (GREEN), extract env-var building to helper (REFACTOR) | scope: src/container-runner.ts, src/container-runner.test.ts, src/host-sweep.ts | success: `pnpm vitest run src/container-runner.test.ts && pnpm run build` | size: M
- [x] Deep work MCP tools in container agent-runner: test DeepWorkState JSON round-trip, test start_deep_work creates state file with correct deadline, test end_deep_work refuses when >30 min remaining, test update_deep_work warns on premature completion keywords (RED), create container/agent-runner/src/mcp-tools/deep-work.ts with DeepWorkState interface, file helpers, 4 tools — start_deep_work (goal + deadline_minutes or deadline_time + plan → writes deep_work.json), update_deep_work (progress + premature-completion warning), end_deep_work (cleanup + >30min refusal), get_deep_work_status (deadline + progress + remaining). Register in server.ts (GREEN), extract deadline parsing utility (REFACTOR) | scope: container/agent-runner/src/mcp-tools/deep-work.ts, container/agent-runner/src/mcp-tools/deep-work.test.ts, container/agent-runner/src/mcp-tools/server.ts | success: `cd container/agent-runner && bun test src/mcp-tools/deep-work.test.ts` | size: M | data_contract: reads/writes deep_work.json in agent working directory
- [x] Deep work auto-continuation in poll loop: test checkDeepWorkContinuation returns null when no state file, returns rich prompt when deadline is future with correct urgency scaling, returns null when expired (RED), implement checkDeepWorkContinuation() — reads deep_work.json, builds continuation prompt with state summary (goal, plan, completed, current, remaining time) + time-scaled urgency nudge (>2h strong, >30m moderate, <10m wrap-up). Wire into poll-loop.ts: after provider query completes and result is written, check continuation, if active wait 3s then re-enter query with continuation prompt (GREEN) | scope: container/agent-runner/src/mcp-tools/deep-work.ts, container/agent-runner/src/poll-loop.ts | success: `cd container/agent-runner && bun test` | size: M
- [x] Deep work container skill: verify no existing deep-work skill (RED), create container/skills/deep-work/SKILL.md — trigger patterns (time budget and deadline language), start_deep_work usage (duration and absolute), work loop rules (never stop early, never wait for user, check clock, update_deep_work after each sub-task), compaction recovery (get_deep_work_status + recall context), wrap-up instructions (last 10 min: finish current, test, commit, end_deep_work, summary report, extract key memories to MEMORY.md), edge cases (GREEN) | scope: container/skills/deep-work/SKILL.md | success: `test -f container/skills/deep-work/SKILL.md && grep -q 'start_deep_work' container/skills/deep-work/SKILL.md && grep -q 'MEMORY' container/skills/deep-work/SKILL.md` | size: S
- [x] Post-session conversation archive and memory hook: test conversation archive produces markdown from mock transcript, test MEMORY.md mtime check detects updates (RED), add PreCompact hook to claude.ts provider — archives conversation to conversations/ in agent working dir (ported from v1 createPreCompactHook). Add post-exit check in container-runner.ts host-mode close handler — after child exits, compare MEMORY.md mtime before/after session, log warning if >10-turn session had no memory writes (GREEN) | scope: container/agent-runner/src/providers/claude.ts, src/container-runner.ts | success: `cd container/agent-runner && bun test && pnpm run build` | size: M | data_contract: reads session transcript, writes conversations/*.md
- [x] Build and regression verification: run full build, all host tests, all container tests, container typecheck, verify container-mode path unchanged — env var defaults match hardcoded values, existing tests pass, no Docker-specific assumptions broken [manual: end-to-end smoke test requires Bun + API credentials] | scope: src/**, container/agent-runner/src/** | success: `pnpm run build && pnpm vitest run && (cd container/agent-runner && bun test) && pnpm exec tsc -p container/agent-runner/tsconfig.json --noEmit` | size: S

</details>

<details>
<summary>Phase 3a: Qwen Worker Baseline Experiments (7/7 done)</summary>

- [x] Experiment runner and log scaffold | scope: scripts/qwen-bench.sh, docs/qwen-experiment-log.md | success: `test -x scripts/qwen-bench.sh && bash scripts/qwen-bench.sh --health && test -f docs/qwen-experiment-log.md` | size: S
- [x] File operations experiments (1-3) | scope: docs/qwen-experiment-log.md | size: M
- [x] Code implementation experiments (4-6) | scope: docs/qwen-experiment-log.md | size: M
- [x] Research synthesis experiments (7-8) | scope: docs/qwen-experiment-log.md | size: S
- [x] Structured output experiments (9-10) | scope: docs/qwen-experiment-log.md | size: S
- [x] Context utilization experiments (11-12) | scope: docs/qwen-experiment-log.md | size: M
- [x] Findings synthesis | scope: docs/qwen-experiment-log.md | size: S

</details>

<details>
<summary>Phase 3b: Dispatch Module Implementation (7/7 done)</summary>

- [x] Contract types and task state management: test TaskContract round-trip serialization with all required fields including context_budget_tokens, test invalid contracts rejected (missing required fields), test task state file read/write for worker-tasks/ and worker-results/ directories (pending → running → completed/failed/timeout transitions) (RED), implement TaskContract/TaskResult/VerificationResult/T0Check/TaskState types and JSON file helpers with atomic write (GREEN) | scope: container/agent-runner/src/mcp-tools/local-worker/contract.ts, container/agent-runner/src/mcp-tools/local-worker/contract.test.ts | success: `cd container/agent-runner && bun test src/mcp-tools/local-worker/contract.test.ts` | size: S
- [x] Prompt builder from contracts: test buildWorkerPrompt produces 4-primitive system+user message pair from contract, test system prompt varies by outputFormat (json → "Output ONLY valid JSON", code → "Output ONLY the code", markdown → plain), test context included in user message and trimmed at context_budget_tokens, test boundaries appear in system prompt (RED), implement prompt builder: contract → { system, user } message pair following 4-primitive format (objective, output format, tool guidance, boundaries) (GREEN) | scope: container/agent-runner/src/mcp-tools/local-worker/prompt-builder.ts, container/agent-runner/src/mcp-tools/local-worker/prompt-builder.test.ts | success: `cd container/agent-runner && bun test src/mcp-tools/local-worker/prompt-builder.test.ts` | size: S
- [x] Result parser with fence stripping: test parseWorkerResult strips markdown code fences from code output, test JSON.parse validation for json outputFormat (valid passes, invalid returns error), test markdown output passes through unchanged, test empty/malformed responses return structured error (RED), implement result parser: detect and strip triple-backtick fences, JSON.parse for json format, structured ParseError for malformed (GREEN) | scope: container/agent-runner/src/mcp-tools/local-worker/result-parser.ts, container/agent-runner/src/mcp-tools/local-worker/result-parser.test.ts | success: `cd container/agent-runner && bun test src/mcp-tools/local-worker/result-parser.test.ts` | size: S
- [x] T0 deterministic verification engine: test each T0Check type — json-valid (valid/invalid JSON), contains (substring match/miss), regex (pattern match/miss), line-count (within/outside min-max bounds); test multiple checks AND-composed (all pass → pass, any fail → fail with per-check details); test empty postconditions trivial pass; test implicit json-valid check when outputFormat=json with no explicit postconditions (RED), implement verification engine: dispatch by check type, compose results, return VerificationResult with per-check pass/fail, add implicit json-valid for json outputFormat (GREEN) | scope: container/agent-runner/src/mcp-tools/local-worker/verification.ts, container/agent-runner/src/mcp-tools/local-worker/verification.test.ts | success: `cd container/agent-runner && bun test src/mcp-tools/local-worker/verification.test.ts` | size: S
- [x] Background dispatch executor with timeout: test executeWorkerTask builds correct request body for llama-cpp /v1/chat/completions with prompt from prompt-builder, test AbortController timeout aborts fetch after deadline and writes timeout status, test successful execution writes completed result to worker-results/<id>.json with parsed output + T0 verification, test network error writes failed result with error message (RED), implement dispatch.ts: executeWorkerTask() — build prompt, fetch llama-cpp endpoint with AbortController(timeout_ms), parse via result-parser, verify via verification engine, write result file; handle fetch errors and timeouts as failed state (GREEN), extract llama-cpp endpoint URL to config.ts constant (REFACTOR) | scope: container/agent-runner/src/mcp-tools/local-worker/dispatch.ts, container/agent-runner/src/mcp-tools/local-worker/dispatch.test.ts, container/agent-runner/src/config.ts | success: `cd container/agent-runner && bun test src/mcp-tools/local-worker/dispatch.test.ts` | size: M | data_contract: reads worker-tasks/<id>.json, writes worker-results/<id>.json
- [x] MCP tools registration and poll-loop auto-pickup: test dispatch_worker tool handler writes task file and returns immediately with task ID, test get_worker_status returns correct state for pending/completed/failed tasks, test cancel_worker marks pending task as cancelled; test checkWorkerResults returns null when no completed results, returns formatted summary when completed/failed results exist, cleans up processed result files after pickup (RED), implement tools.ts with 3 MCP tools (dispatch_worker: build contract + write task + fire-and-forget executeWorkerTask + return ID, get_worker_status: read task/result files, cancel_worker: write cancelled state); add checkWorkerResults() to poll-loop.ts scanning worker-results/ for completed/failed, formatting injection prompt, cleaning processed files; wire after deep-work continuation check; create index.ts barrel with registerTools() (GREEN), clean up result formatting (REFACTOR) | scope: container/agent-runner/src/mcp-tools/local-worker/tools.ts, container/agent-runner/src/mcp-tools/local-worker/tools.test.ts, container/agent-runner/src/mcp-tools/local-worker/poll-integration.test.ts, container/agent-runner/src/mcp-tools/local-worker/index.ts, container/agent-runner/src/poll-loop.ts | success: `cd container/agent-runner && bun test` | size: M
- [x] Container skill and build verification: verify no local-worker skill exists (RED), create container/skills/local-worker/SKILL.md with dispatch patterns — when to dispatch vs handle yourself, task type mapping to contract types, auto-pickup mental model (results surface automatically via poll loop, get_worker_status for manual check only), review workflow after result arrives, multi-worker coordination tips; add import './local-worker/index.js' to mcp-tools barrel (GREEN) | scope: container/skills/local-worker/SKILL.md, container/agent-runner/src/mcp-tools/index.ts | success: `test -f container/skills/local-worker/SKILL.md && grep -q 'dispatch_worker' container/skills/local-worker/SKILL.md && cd container/agent-runner && bun test && bun run typecheck` | size: S

</details>

<details>
<summary>Phase 3c: Dispatch Integration + Iteration (7/7 done)</summary>

- [x] Host model registry and config injection: test loadModels returns parsed ModelConfig array from fixture JSON, test loadModels returns empty array when file missing, test buildLocalWorkerConfig produces correct routing table shape from ModelConfig array mapping task_types to urls (RED), implement model-registry.ts with ModelConfig type (name, url, max_concurrent, task_types[]) and loadModels(groupDir), implement buildLocalWorkerConfig that transforms ModelConfig[] into routing table for container, create index.ts exporting both, add side-effect import to src/modules/index.ts, wire into container-runner.ts to inject localWorker config into containerConfig at spawn (GREEN), extract models.json path constant (REFACTOR) | scope: src/modules/local-worker/model-registry.ts, src/modules/local-worker/model-registry.test.ts, src/modules/local-worker/index.ts, src/modules/index.ts, src/container-runner.ts | success: `pnpm vitest run src/modules/local-worker/model-registry.test.ts && pnpm run build` | size: M
- [x] Container routing config and dispatch update: test routeTask returns configured URL for 'code-impl' type from injected config, test routeTask returns LLAMA_CPP_URL as fallback when no localWorker config present, test routeTask returns default URL for unmapped task types (RED), implement routing.ts with loadRoutingConfig (reads localWorker field from container.json) and routeTask(taskType) → url with fallback, update executeWorkerTask to call routeTask internally instead of hardcoded LLAMA_CPP_URL, extend RunnerConfig in config.ts with optional localWorker field (GREEN), extract routing config type to contract.ts (REFACTOR) | scope: container/agent-runner/src/mcp-tools/local-worker/routing.ts, container/agent-runner/src/mcp-tools/local-worker/routing.test.ts, container/agent-runner/src/mcp-tools/local-worker/dispatch.ts, container/agent-runner/src/config.ts | success: `cd container/agent-runner && bun test src/mcp-tools/local-worker/routing.test.ts` | size: S
- [x] Per-endpoint concurrency semaphore: test Semaphore(1) blocks second call until first resolves, test Semaphore(2) allows two concurrent calls, test queued requests execute FIFO after release (RED), implement Semaphore class in semaphore.ts (acquire returns Promise, release unblocks next queued), update executeWorkerTask to acquire semaphore before fetch and release in finally block, read max_concurrent from routing config defaulting to 1 (GREEN) | scope: container/agent-runner/src/mcp-tools/local-worker/semaphore.ts, container/agent-runner/src/mcp-tools/local-worker/semaphore.test.ts, container/agent-runner/src/mcp-tools/local-worker/dispatch.ts | success: `cd container/agent-runner && bun test src/mcp-tools/local-worker/semaphore.test.ts` | size: S
- [x] E2E test harness with ground truth: test harness defines 10 task fixtures with expected_verdict field, test --dry-run mode loads fixtures and validates contract shapes without calling llama-cpp, test results JSON schema has required fields (task_id, type, latency_ms, t0_passed, expected_verdict, actual_verdict, raw, parsed) (RED), implement scripts/worker-e2e.ts dispatching 10 tasks (3 code-impl, 3 structured-output, 2 research, 2 file-op) with ground truth expected_verdict per fixture, capture all metrics, compute FP/FN rates, write results to docs/worker-e2e-results.json + summary to stdout (GREEN), extract fixture definitions to scripts/worker-e2e-fixtures.ts (REFACTOR) | scope: scripts/worker-e2e.ts, scripts/worker-e2e-fixtures.ts, docs/worker-e2e-results.json | success: `test -f scripts/worker-e2e.ts && bun run scripts/worker-e2e.ts --dry-run 2>&1 | grep -q 'fixtures loaded: 10'` | size: M
- [x] Run E2E and prompt template refinement: run E2E harness against live llama-cpp, identify tasks where actual_verdict != expected_verdict (RED), for each failure pattern update prompt-builder.ts FORMAT_INSTRUCTIONS or buildWorkerPrompt logic — add type-specific prompt shaping if research/code show different failure modes (GREEN), extract new format patterns to constants (REFACTOR) | scope: container/agent-runner/src/mcp-tools/local-worker/prompt-builder.ts, scripts/worker-e2e.ts, docs/worker-e2e-results.json | success: `bun run scripts/worker-e2e.ts 2>&1 | grep -q 'pass rate' && bun run scripts/worker-e2e.ts 2>&1 | grep -v 'FP rate: [1-9]'` | size: M | data_contract: reads docs/worker-e2e-results.json, writes prompt-builder.ts
- [x] Verification calibration and regression: identify T0 checks from docs/worker-e2e-results.json that produced false positives or false negatives, write tests for any new check type needed (RED), implement adjustments to verification.ts — add new check types if needed (e.g. not-contains for anti-patterns), tune existing params, re-run E2E and confirm FP+FN rate reduced (GREEN) | scope: container/agent-runner/src/mcp-tools/local-worker/verification.ts, container/agent-runner/src/mcp-tools/local-worker/verification.test.ts, docs/worker-e2e-results.json | success: `cd container/agent-runner && bun test src/mcp-tools/local-worker/verification.test.ts` | size: S
- [x] Build verification and skill update: verify current SKILL.md does not mention model routing (RED), add "Model Routing" section to SKILL.md explaining task type routing and when different models are appropriate, run full build + all test suites + container typecheck (GREEN) | scope: container/skills/local-worker/SKILL.md, container/agent-runner/src/mcp-tools/local-worker/** | success: `pnpm run build && pnpm vitest run && cd container/agent-runner && bun test && bun run typecheck && grep -q 'Model Routing' container/skills/local-worker/SKILL.md` | size: S

</details>

<details>
<summary>Phase 4: Voice I/O (5/5 done)</summary>

- [x] Voice toolchain prerequisites: verify whisper-server not reachable and edge-tts CLI not available (RED), install whisper.cpp via Homebrew + download whisper-large-v3-turbo model + install edge-tts via pip3 + configure WHISPER_URL in .env, verify whisper-server responds and edge-tts generates audio (GREEN) | scope: .env | success: `grep -q WHISPER_URL .env && edge-tts --help > /dev/null 2>&1` | size: S
- [x] STT transcription function with fixture test: test transcribeAudio returns non-empty text from mock whisper-server response using real .ogg fixture file, test returns null when server unreachable (graceful degradation), test isAudioFile correctly identifies ogg/opus/m4a/wav/mp3/webm extensions (RED), create test fixture .ogg via ffmpeg, implement stt.ts with transcribeAudio(filePath) — POST multipart/form-data to WHISPER_URL /v1/audio/transcriptions, parse JSON response; implement isAudioFile(filename) extension matcher (GREEN), extract AUDIO_EXTENSIONS constant (REFACTOR) | scope: src/modules/voice/stt.ts, src/modules/voice/stt.test.ts, src/modules/voice/fixtures/test.ogg | success: `pnpm vitest run src/modules/voice/stt.test.ts` | size: M
- [x] STT router integration and module registration: test processVoiceAttachments detects audio attachment by filename and prepends "[Voice transcription: ...]" to message text, test non-audio attachments pass through unchanged, test transcription failure preserves original message with audio attachment intact (RED), implement router-hook.ts with processVoiceAttachments(contentStr, inboxDir) that scans attachments for audio files transcribes via stt.ts prepends text; create index.ts barrel exporting processVoiceAttachments, add import to src/modules/index.ts, wire call in session-manager.ts writeInboundMessage after extractAttachmentFiles, add WHISPER_URL to env.ts (GREEN) | scope: src/modules/voice/router-hook.ts, src/modules/voice/router-hook.test.ts, src/modules/voice/index.ts, src/modules/index.ts, src/session-manager.ts, src/env.ts | success: `pnpm vitest run src/modules/voice/ && pnpm run build` | size: M
- [x] TTS MCP tool with edge-tts subprocess: test speak tool handler returns audio file path and writes mp3 to outbox directory, test edge-tts subprocess invocation builds correct CLI args with voice selection, test graceful error when edge-tts binary not found, test default voice selection (RED), implement voice-tts.ts with speak(text, voice?) MCP tool — spawns edge-tts CLI subprocess with --text and --write-media args, writes mp3 to outbox/<id>.mp3, returns success with file reference; register in server.ts (host-mode: Python available on host; Docker deferred) (GREEN), extract voice list constant and subprocess helper (REFACTOR) | scope: container/agent-runner/src/mcp-tools/voice-tts.ts, container/agent-runner/src/mcp-tools/voice-tts.test.ts, container/agent-runner/src/mcp-tools/server.ts | success: `cd container/agent-runner && bun test src/mcp-tools/voice-tts.test.ts` | size: M
- [x] Voice skill and build verification: verify no voice skill exists and container barrel doesn't import voice-tts (RED), create container/skills/voice/SKILL.md with speak() usage, voice selection guide, audio message handling context; add import to mcp-tools barrel; run full build + all test suites + container typecheck (GREEN) | scope: container/skills/voice/SKILL.md, container/agent-runner/src/mcp-tools/index.ts | success: `pnpm run build && pnpm vitest run && cd container/agent-runner && bun test && bun run typecheck && test -f container/skills/voice/SKILL.md` | size: S

</details>

<details>
<summary>Phase 5: Web Search + Deep Research (5/5 done)</summary>

- [x] web_search MCP tool with SearXNG backend: test web_search returns normalized {title, url, snippet}[] from mock SearXNG JSON response, test max_results limits output, test graceful error when SEARXNG_URL not configured or unreachable (RED), implement web-search.ts fetching SEARXNG_URL/search?format=json&q=..., normalize results to SearchResult[], register tool via registerTools(); add SEARXNG_URL to config.ts as env var with fallback (GREEN), extract result normalization helper (REFACTOR) | scope: container/agent-runner/src/mcp-tools/web-search.ts, container/agent-runner/src/mcp-tools/web-search.test.ts, container/agent-runner/src/config.ts | success: `cd container/agent-runner && bun test src/mcp-tools/web-search.test.ts` | size: M
- [x] web_extract MCP tool with readability extraction: test web_extract returns markdown from mock HTML fixture with title and body, test content truncation at max_chars parameter, test graceful error on fetch failure, test non-HTML content type returns raw text (RED), install @mozilla/readability and linkedom in container/agent-runner via bun install (commit bun.lock), implement web-extract.ts with fetch + linkedom DOM parse + readability extract → clean markdown, register tool (GREEN) | scope: container/agent-runner/src/mcp-tools/web-extract.ts, container/agent-runner/src/mcp-tools/web-extract.test.ts, container/agent-runner/package.json, container/agent-runner/bun.lock | success: `cd container/agent-runner && bun test src/mcp-tools/web-extract.test.ts` | size: M
- [x] wiki_write MCP tool with dynamic routing: test reads wikis.json fixture and routes "AML fraud detection" topic to aml-wiki path, test routes "SQLite optimization" to database-wiki path, test explicit wiki_name parameter overrides auto-routing, test unmatched topic falls back to first wiki, test output file has valid YAML frontmatter with required fields (title, tags, source, created) (RED), implement wiki-write.ts reading WIKIS_JSON_PATH (defaults ~/.claude/wikis.json, host-mode reads directly), optional wiki_name for explicit routing, keyword scoring against wiki descriptions for auto-routing, writes markdown with frontmatter to best-match wiki inbox/, register tool (GREEN) | scope: container/agent-runner/src/mcp-tools/wiki-write.ts, container/agent-runner/src/mcp-tools/wiki-write.test.ts | success: `cd container/agent-runner && bun test src/mcp-tools/wiki-write.test.ts` | size: M
- [x] Deep research container skill + barrel wiring + pipeline test: verify no deep-research skill exists and barrel doesn't import web-search/web-extract/wiki-write (RED), create SKILL.md with multi-step research workflow (search → extract → worker synthesis → wiki write), structured output format, source citation rules, Qwen dispatch patterns, compaction recovery guidance; add imports to mcp-tools barrel index.ts; write pipeline integration test verifying mock web_search results serialize into dispatch_worker research contract with web content as context, contract validates, output format compatible with wiki_write (GREEN) | scope: container/skills/deep-research/SKILL.md, container/agent-runner/src/mcp-tools/index.ts, container/agent-runner/src/mcp-tools/web-research-pipeline.test.ts | success: `test -f container/skills/deep-research/SKILL.md && grep -q 'web_search' container/skills/deep-research/SKILL.md && grep -q 'wiki_write' container/skills/deep-research/SKILL.md && grep -q 'web-search' container/agent-runner/src/mcp-tools/index.ts && cd container/agent-runner && bun test src/mcp-tools/web-research-pipeline.test.ts` | size: M
- [x] Build verification and regression: verify all three new MCP tools are importable and no build errors from new deps (RED), run full build + all host tests + all container tests + container typecheck (GREEN) | scope: container/agent-runner/src/mcp-tools/**, container/skills/deep-research/** | success: `pnpm run build && pnpm vitest run && cd container/agent-runner && bun test && bun run typecheck` | size: S

</details>

<details>
<summary>Phase 6a: Worker Tool-Calling Runtime (9/9 done)</summary>

- [x] Tool-call experiment harness and health check: verify no tool-call bench script exists (RED), create scripts/tool-call-bench.ts with llama-cpp tool-calling health check — send single request with `tools` param to /v1/chat/completions, verify response contains tool_calls array or content; include --dry-run flag that validates script structure without requiring live server; create docs/tool-call-experiment-log.md scaffold with experiment template (GREEN) | scope: scripts/tool-call-bench.ts, docs/tool-call-experiment-log.md | success: `test -f scripts/tool-call-bench.ts && test -f docs/tool-call-experiment-log.md && bun run scripts/tool-call-bench.ts --dry-run 2>&1 | grep -q 'ok'` | size: S
- [x] Basic tool-calling experiments (1-4): single tool availability and correct invocation format, parameter extraction accuracy from natural language, response format discovery (OpenAI function_call vs Hermes-style XML tags vs grammar-constrained), comparison of format reliability across approaches | scope: docs/tool-call-experiment-log.md | success: `grep -c '## Experiment [1-4]' docs/tool-call-experiment-log.md | grep -q '^4$' && grep -q 'verdict:' docs/tool-call-experiment-log.md` | size: M
- [x] Multi-tool and multi-turn experiments (5-8): multiple tools available with correct routing to intended tool, distractor tool rejection (does NOT call irrelevant tools), tool result injection into context with correct follow-up reasoning, chained tool calls where output of tool A feeds input of tool B | scope: docs/tool-call-experiment-log.md | success: `grep -c '## Experiment [5-8]' docs/tool-call-experiment-log.md | grep -q '^4$'` | size: M
- [x] Termination and failure mode experiments (9-12): model knows when to stop calling tools and produce final output, handles tool execution errors gracefully (continues or retries), context budget degradation across 5+ turns, hallucinated tool names or malformed parameters | scope: docs/tool-call-experiment-log.md | success: `test $(grep -c '## Experiment' docs/tool-call-experiment-log.md) -ge 12` | size: M
- [x] Findings synthesis and go/no-go gate: document working format (best tool-calling approach for this model), recommended prompt template for tool definitions, fallback strategies for failure modes, parameter limits (max tools, max iterations, context budget); **decision gate: if overall pass rate <60%, pivot to regex-parsed XML tag approach before proceeding** | scope: docs/tool-call-experiment-log.md | success: `grep -q '## Recommendations' docs/tool-call-experiment-log.md && grep -q 'pass rate:' docs/tool-call-experiment-log.md` | size: S
- [x] Worker tool registry: test buildToolDefinitions returns correct tool definitions subset from contract's tools field, test unknown tool name returns error, test empty tools field returns empty array (preserves single-shot mode) (RED), implement tool-registry.ts with buildToolDefinitions(allowedTools: string[]) that looks up registered MCP tool handlers by name and builds tool definition objects matching the format chosen in experiments (GREEN), extract tool schema constants (REFACTOR) | scope: container/agent-runner/src/mcp-tools/local-worker/tool-registry.ts, container/agent-runner/src/mcp-tools/local-worker/tool-registry.test.ts | success: `cd container/agent-runner && bun test src/mcp-tools/local-worker/tool-registry.test.ts` | size: M
- [x] Worker agent loop: test loop returns final text when model produces no tool calls, test loop executes single tool and re-queries with result, test loop handles chained tool calls (2+ iterations), test loop terminates at max_iterations with partial result, test loop terminates at timeout_ms with partial result, test loop handles tool execution error gracefully (continues with error message in context) (RED), implement agent-loop.ts with executeAgentLoop({messages, tools, maxIterations, timeoutMs}) running multi-turn cycle — inference → parse tool_calls → execute via registry → append result → repeat until text response or limit reached (GREEN), extract message formatting helper (REFACTOR) | scope: container/agent-runner/src/mcp-tools/local-worker/agent-loop.ts, container/agent-runner/src/mcp-tools/local-worker/agent-loop.test.ts | success: `cd container/agent-runner && bun test src/mcp-tools/local-worker/agent-loop.test.ts` | size: L
- [x] Contract extension and dispatch integration: test contract with tools field validates correctly, test contract without tools field still validates (backward compat), test executeWorkerTask routes to agent loop when tools present, test executeWorkerTask routes to single-shot when no tools, test semaphore released between inference calls within agent loop (not held for entire loop), test two concurrent workers dispatched simultaneously don't overlap inference calls (RED), extend TaskContract type with optional `tools: string[]` field, update executeWorkerTask to branch: if tools present → build tool definitions via registry → executeAgentLoop, else → existing single-shot path; modify semaphore to acquire/release per-inference-call inside agent loop (GREEN) | scope: container/agent-runner/src/mcp-tools/local-worker/contract.ts, container/agent-runner/src/mcp-tools/local-worker/contract.test.ts, container/agent-runner/src/mcp-tools/local-worker/dispatch.ts, container/agent-runner/src/mcp-tools/local-worker/dispatch.test.ts, container/agent-runner/src/mcp-tools/local-worker/semaphore.ts | success: `cd container/agent-runner && bun test src/mcp-tools/local-worker/contract.test.ts && bun test src/mcp-tools/local-worker/dispatch.test.ts` | size: M
- [x] E2E integration test and skill update: verify current SKILL.md does not mention tool-calling (RED), write integration test that dispatches a worker with tools field, mocks tool execution, verifies result file lands in worker-results/ with tool trace; add "Tool-Calling Workers" section to SKILL.md — when to dispatch with tools, available tool names, multi-turn mental model, max_iterations guidance, reviewing tool traces; run full build + all test suites + container typecheck (GREEN) | scope: container/agent-runner/src/mcp-tools/local-worker/agent-loop-e2e.test.ts, container/skills/local-worker/SKILL.md, container/agent-runner/src/mcp-tools/local-worker/** | success: `grep -q 'Tool-Calling' container/skills/local-worker/SKILL.md && pnpm run build && pnpm vitest run && cd container/agent-runner && bun test && bun run typecheck` | size: S

</details>

<details>
<summary>Phase 6b: Research Loop + Episodic Integration (6/6 done)</summary>

- [x] Wire dispatch_worker tools field: test dispatch_worker with tools param constructs TaskContract with tools array (currently silently dropped), test dispatch_worker MCP inputSchema includes tools property (RED), add tools property to dispatch_worker inputSchema in index.ts with type array of strings, wire args.tools as optional string[] through to TaskContract in handleDispatchWorker (GREEN) | scope: container/agent-runner/src/mcp-tools/local-worker/tools.ts, container/agent-runner/src/mcp-tools/local-worker/tools.test.ts, container/agent-runner/src/mcp-tools/local-worker/index.ts | success: `cd container/agent-runner && bun test src/mcp-tools/local-worker/tools.test.ts` | size: S
- [x] wiki_write episodic tier and provenance: test wiki_write with tier='episodic' writes to episodic/ subdirectory not inbox/, test frontmatter includes worker_id and task_id when provided, test default behavior unchanged (no tier param writes to inbox/), test episodic frontmatter has source: worker-research and tier: episodic (RED), add optional tier, worker_id, task_id params to wiki_write inputSchema and writeHandler, route to episodic/ when tier='episodic', include provenance fields in buildFrontmatter (GREEN), extract directory routing logic to helper function (REFACTOR) | scope: container/agent-runner/src/mcp-tools/wiki-write.ts, container/agent-runner/src/mcp-tools/wiki-write.test.ts | success: `cd container/agent-runner && bun test src/mcp-tools/wiki-write.test.ts` | size: M
- [x] Research loop container skill: verify no research-loop skill directory exists (RED), create container/skills/research-loop/SKILL.md with scheduled research workflow — trigger patterns, dispatch template with tools [web_search, web_extract, wiki_write] and context budget 6000 tokens and max_iterations 6 and web_extract max_chars 8000, context shaping guidance (strip orchestrator reasoning, include only topic + output format + boundaries), max_duration handling (derive worker timeout_ms from task duration, check elapsed time before each dispatch), episodic tier usage with provenance, channel notification format after completion, compaction recovery (GREEN) | scope: container/skills/research-loop/SKILL.md | success: `test -f container/skills/research-loop/SKILL.md && grep -q 'web_search' container/skills/research-loop/SKILL.md && grep -q 'episodic' container/skills/research-loop/SKILL.md && grep -q 'max_duration' container/skills/research-loop/SKILL.md` | size: S
- [x] Scheduled research end-to-end integration test: test worker dispatched with tools [web_search, web_extract, wiki_write] executes multi-turn research pipeline with mocked tool responses, test wiki_write called with tier='episodic' and worker_id + task_id provenance, test result file in worker-results/ includes tool trace, test worker result contains wiki output path for orchestrator notification (RED), implement integration test mocking tool registry to return controlled responses — web_search returns URLs, web_extract returns content, wiki_write writes episodic entry; verify episodic file has correct YAML frontmatter, verify result includes wiki file path (GREEN) | scope: container/agent-runner/src/mcp-tools/local-worker/research-loop.test.ts | success: `cd container/agent-runner && bun test src/mcp-tools/local-worker/research-loop.test.ts` | size: M
- [x] Max_duration and recurrence verification: test agent loop with timeout_ms produces partial result file with clean exit (not lost, no exception), write dedicated recurrence test with research-task-style content verifying task fires, completes, and new pending row inserted with same series_id (RED), verify agent-loop timeout test covers clean-exit scenario, implement research-content recurrence test using scheduling/db helpers (GREEN) | scope: container/agent-runner/src/mcp-tools/local-worker/agent-loop.test.ts, src/modules/scheduling/recurrence.test.ts | success: `cd container/agent-runner && bun test src/mcp-tools/local-worker/agent-loop.test.ts && pnpm vitest run src/modules/scheduling/recurrence.test.ts` | size: S
- [x] Build verification and regression: verify wiki_write MCP schema includes tier, worker_id, task_id params and dispatch_worker schema includes tools param, run full build + all host tests + all container tests + container typecheck | scope: container/agent-runner/src/mcp-tools/**, src/modules/scheduling/** | success: `pnpm run build && pnpm vitest run && cd container/agent-runner && bun test && bun run typecheck` | size: S

</details>

<details>
<summary>Phase 7: Production Hardening + Wiki Bridge (5/5 done)</summary>

- [x] Wiki domain map generation: test generateWikiContext reads fixture wikis.json + fixture schema.md hierarchy roots section, produces wiki-context.md with wiki names, descriptions, hierarchy roots, and article counts (recursive walk of articles/ subdirs); test returns no-op when wikis.json missing; test skips wiki with missing path and logs warning; test fragment stays under ~500 tokens per wiki (RED), implement wiki-bridge.ts in src/modules/memory/ — reads WIKIS_JSON_PATH (default ~/.claude/wikis.json), for each wiki reads schema.md "## Hierarchy Roots" section and counts .md files recursively in articles/, renders .claude-fragments/wiki-context.md; export generateWikiContext from memory/index.ts (GREEN), extract schema.md hierarchy parser to helper function (REFACTOR) | scope: src/modules/memory/wiki-bridge.ts, src/modules/memory/wiki-bridge.test.ts, src/modules/memory/index.ts | success: `pnpm vitest run src/modules/memory/wiki-bridge.test.ts` | size: M
- [x] Spawn pipeline wiki context integration: verify container-runner.ts does not yet call generateWikiContext (RED), add generateWikiContext(groupDir) call alongside existing generateMemoryFragment(groupDir) in both buildMounts (container path) and wakeHostRunner (host-mode path) in container-runner.ts, wrapped in same try/catch pattern; verify WIKI_TOOLS_DIR propagates from process.env through buildHostRunnerEnv spread (GREEN) | scope: src/container-runner.ts | success: `pnpm run build && grep -q 'generateWikiContext' src/container-runner.ts` | size: S
- [x] wiki_search MCP tool with search.py integration: test wiki_search with mock search.py subprocess returns ranked {slug, title}[] results, test wiki_search with no .wiki-index.db falls back to keyword search over article frontmatter titles and tags, test keyword search scores title matches higher than tag matches, test wiki_search with missing WIKIS_JSON_PATH returns structured error, test wiki_search with explicit wiki_name param routes to correct wiki, test wiki_search with missing WIKI_TOOLS_DIR skips search.py tier, test corrupt/non-JSON subprocess stdout returns graceful error (RED), implement wiki-search.ts — resolve wiki path from wikis.json (reuse pattern from wiki-write.ts), check .wiki-index.db exists, if yes spawn python3 search.py query subprocess and parse stdout, if no scan articles/ dir recursively reading frontmatter titles and tags for keyword matches; register wiki_search tool in server.ts; add import to mcp-tools barrel (GREEN), extract keyword scoring to standalone function (REFACTOR) | scope: container/agent-runner/src/mcp-tools/wiki-search.ts, container/agent-runner/src/mcp-tools/wiki-search.test.ts, container/agent-runner/src/mcp-tools/index.ts | success: `cd container/agent-runner && bun test src/mcp-tools/wiki-search.test.ts` | size: M
- [x] Automated spawn pipeline smoke test: verify no smoke-test script exists in scripts/ (RED), implement scripts/smoke-test.ts — creates temp group dir with fixture MEMORY.md + container.json + wikis.json + fixture schema.md, creates temp session dir, exercises: (1) generateMemoryFragment → verify memory-context.md exists and non-empty, (2) generateWikiContext → verify wiki-context.md exists with expected wiki names, (3) composeGroupClaudeMd → verify composed output includes fragments, (4) buildHostRunnerEnv → verify env record contains expected keys; output structured pass/fail per step; cleanup temp dirs; --dry-run flag validates script structure (GREEN) | scope: scripts/smoke-test.ts | success: `bun run scripts/smoke-test.ts --dry-run && echo PASS` | size: M
- [x] Build verification and regression: verify wiki_search and wiki-bridge imports present in respective barrels (RED), run full build + all host tests + all container tests + container typecheck (GREEN) | scope: container/agent-runner/src/mcp-tools/**, src/modules/memory/** | success: `pnpm run build && pnpm vitest run && cd container/agent-runner && bun test && bun run typecheck` | size: S

</details>

<details>
<summary>Phase 8: Operational Deployment + E2E Validation (6/6 done)</summary>

- [x] Agent group host-mode configuration: verify dm-with-wang and cli-with-wang container.json lack "provider" field and default to container mode, verify no memory/MEMORY.md exists in group dirs (RED), add `"provider": "host"` to both container.json files, create `groups/dm-with-wang/memory/MEMORY.md` with 3 seed entries (user profile, preferences, role), add NANOCLAW_LLAMA_URL and SEARXNG_URL to .env (GREEN) | scope: groups/*/container.json, groups/dm-with-wang/memory/MEMORY.md, .env | success: `jq -e '.provider == "host"' groups/dm-with-wang/container.json && jq -e '.provider == "host"' groups/cli-with-wang/container.json && test -f groups/dm-with-wang/memory/MEMORY.md && wc -l < groups/dm-with-wang/memory/MEMORY.md | xargs test 3 -lt && grep -q 'NANOCLAW_LLAMA_URL' .env` | size: S
- [x] Host-mode E2E via CLI adapter: after Task 1 config applied, start host process, send test message through CLI adapter, verify host-mode runner spawns and produces coherent response (RED), if spawn fails diagnose and fix — expected issues: OneCLI CA cert not available to host Bun process, bun module resolution from group CWD, session DB creation at NANOCLAW_SESSION_DIR path (GREEN), document any host-mode-specific fixes in commit notes (REFACTOR) | scope: src/container-runner.ts, container/agent-runner/src/index.ts, container/agent-runner/src/db/connection.ts, .env | success: `grep -q 'Spawning host-mode runner' logs/nanoclaw.log && grep -q 'Host runner exited' logs/nanoclaw.log` — manual verification: coherent response received via CLI | size: M
- [x] Telegram message delivery E2E: verify Telegram bot token in .env and messaging_group wired to dm-with-wang in central DB (RED), send Telegram message to bot, verify message routes through adapter to host-mode spawn, response written to outbound.db and delivered back to Telegram (GREEN), document adapter config if any fixes needed (REFACTOR) | scope: src/channels/telegram.ts, src/delivery.ts, src/router.ts | success: `grep -q 'telegram' logs/nanoclaw.log` — manual verification: Telegram chat shows bot response | size: M
- [x] Local worker dispatch E2E: pre-check llama-cpp availability via `curl -sf http://localhost:8080/v1/models`, create groups/dm-with-wang/models.json with endpoint config {name: "qwen3-30b", url: "http://localhost:8080/v1/chat/completions", max_concurrent: 1, task_types: ["code-impl", "structured-output", "research"]} (RED), in host-mode session have agent dispatch a structured-output task via dispatch_worker, verify result file in worker-results/ with T0 verification passing (GREEN) | scope: groups/dm-with-wang/models.json, container/agent-runner/src/mcp-tools/local-worker/** | success: `curl -sf http://localhost:8080/v1/models > /dev/null && ls groups/dm-with-wang/worker-results/*.json 2>/dev/null | head -1 | xargs test -f` — manual verification: worker result delivered to agent | size: M
- [x] Memory persistence across sessions: in host-mode session instruct agent to remember a specific fact, verify MEMORY.md updated in groups/dm-with-wang/memory/ (RED), end session, start new session, verify .claude-fragments/memory-context.md includes the new entry and agent can recall it (GREEN) | scope: src/modules/memory/**, groups/dm-with-wang/memory/MEMORY.md, groups/dm-with-wang/.claude-fragments/memory-context.md | success: `grep -q '.' groups/dm-with-wang/.claude-fragments/memory-context.md && wc -l < groups/dm-with-wang/memory/MEMORY.md | xargs test 3 -lt` — manual verification: agent demonstrates recall in new session | size: S
- [x] Research loop pipeline E2E: pre-check SearXNG availability via `curl -sf "$SEARXNG_URL/search?q=test&format=json"`, if not running set up SearXNG (RED), in host-mode session trigger research task dispatching worker with tools [web_search, web_extract, wiki_write], verify episodic entry appears in knowledge-wiki episodic/ with frontmatter including source: worker-research and tier: episodic (GREEN), verify wiki_search can find the new entry (REFACTOR) | scope: container/agent-runner/src/mcp-tools/web-search.ts, container/agent-runner/src/mcp-tools/wiki-write.ts, container/agent-runner/src/mcp-tools/wiki-search.ts, .env | success: `grep -oP '(?<=SEARXNG_URL=).*' .env | xargs -I{} curl -sf "{}/search?q=test&format=json" > /dev/null` — manual verification: episodic wiki entry with correct frontmatter | size: M

</details>

<details>
<summary>Phase 9: Agent Prompt Reconciliation (5/5 done)</summary>

- [x] Rewrite container/CLAUDE.md — fork-owned base prompt rewrite: verify current file contains "/workspace/agent" hardcoded path and "put into CLAUDE.local.md" memory instructions (RED), rewrite container/CLAUDE.md — remove Memory section entirely (deferred to memory skill fragment), replace Workspace "/workspace/agent" with path-agnostic language, narrow CLAUDE.local.md role to "per-group personality overrides and group-specific config" only, keep Communication and Conversation history sections with path-agnostic references (GREEN) | scope: container/CLAUDE.md | success: `! grep -q '/workspace/agent' container/CLAUDE.md && ! grep -qi 'put into CLAUDE.local.md' container/CLAUDE.md && grep -q 'CLAUDE.local.md' container/CLAUDE.md && grep -q 'Communication' container/CLAUDE.md` | size: S
- [x] Memory skill sole authority: verify instructions.md contains "Coexistence with CLAUDE.local.md" section and SKILL.md contains coexistence language (RED), remove Coexistence section from instructions.md and SKILL.md, replace with clear sole-authority statement in instructions.md, add "Conversation Recall" section explaining when to search conversations/ for historical context vs MEMORY.md for structured persistent facts (GREEN) | scope: container/skills/memory/instructions.md, container/skills/memory/SKILL.md | success: `! grep -q 'Coexistence' container/skills/memory/instructions.md && ! grep -qi 'coexist' container/skills/memory/SKILL.md && grep -q 'conversations' container/skills/memory/instructions.md` | size: S
- [x] Wire SOUL.md as inline fragment in composition pipeline: test composeGroupClaudeMd output for fixture group does not include soul fragment in .claude-fragments/ (RED), in composeGroupClaudeMd read SOUL.md from project root, write content as inline fragment (not symlink) to .claude-fragments/soul.md, add to desired fragments map so composed entry point includes @./.claude-fragments/soul.md import (GREEN), extract SOUL.md path resolution to named constant (REFACTOR) | scope: src/claude-md-compose.ts, src/claude-md-compose.test.ts | success: `pnpm run build && pnpm vitest run src/claude-md-compose` | size: M
- [x] Compose coherence test: write vitest test calling composeGroupClaudeMd with fixture group, asserting soul fragment exists in .claude-fragments/, memory skill fragment exists, shared base has no "/workspace/agent", shared base has no "put into CLAUDE.local.md", composed entry point imports soul.md, no "/workspace/agent" in any container/skills/**/*.md file (RED), make all assertions pass (GREEN), extract assertion helpers for reuse (REFACTOR) | scope: src/claude-md-compose.test.ts, container/skills/**/*.md | success: `pnpm vitest run src/claude-md-compose` | size: M
- [x] Codebase cleanup and build verification: grep codebase for stale "CLAUDE.local.md" memory references and "/workspace/agent" in container/skills/ outside edited files (RED), clean up stale references in agent-runner comments and skill files, verify CLAUDE.local.md per group contains only personality/config, run full build + all host tests + container tests + container typecheck (GREEN) | scope: groups/*/CLAUDE.local.md, container/agent-runner/src/**, container/skills/**/*.md | success: `! grep -rn '/workspace/agent' container/skills/ --include='*.md' | grep -q . && pnpm run build && pnpm vitest run && cd container/agent-runner && bun test && bun run typecheck` | size: S

</details>

<details>
<summary>Phase 10: Host-Mode Integration Tests (4/4 done)</summary>

- [x] Promote spawn pipeline smoke-test to vitest: verify scripts/smoke-test.ts passes standalone via `npx tsx scripts/smoke-test.ts` (RED), convert smoke-test logic into src/spawn-pipeline.test.ts — import composeGroupClaudeMd, generateMemoryFragment, generateWikiContext, buildHostRunnerEnv; create temp group dir with fixture MEMORY.md, container.json, wikis.json, schema.md; assert memory-context.md exists and non-empty, wiki-context.md exists with expected wiki names, composed CLAUDE.md includes fragments, host env contains expected keys (GREEN), extract fixture creation helpers to shared test utility (REFACTOR) | scope: src/spawn-pipeline.test.ts, scripts/smoke-test.ts | success: `pnpm vitest run src/spawn-pipeline.test.ts && grep -q 'composeGroupClaudeMd' src/spawn-pipeline.test.ts` | size: S
- [x] Session DB round-trip test: write vitest test creating temp inbound.db + outbound.db using raw SQL schema matching src/db/sessions.ts pragmas, assert message write to inbound messages_in table is readable, assert message write to outbound messages_out table is readable, assert seq parity convention (host=even, container=odd), assert PRAGMA journal_mode returns delete (RED), implement with correct schema + pragmas (journal_mode=DELETE), insert/read messages_in and messages_out, verify seq parity enforcement, verify journal_mode pragma (GREEN), extract DB fixture creation helpers to test utility (REFACTOR) | scope: src/session-roundtrip.test.ts, src/db/sessions.ts | success: `pnpm vitest run src/session-roundtrip.test.ts && grep -q 'journal_mode' src/session-roundtrip.test.ts` | size: M
- [x] Delivery action dispatch test: write vitest test asserting delivery module correctly parses schedule_message system action from an outbound.db row with mocked channel adapter (RED), implement test: create temp outbound.db with a messages_out row containing system action JSON, import delivery action parsing logic, verify schedule_message action is correctly extracted and would route to scheduling module, verify unknown actions are handled gracefully (GREEN), extract outbound.db row builder to test utility (REFACTOR) | scope: src/delivery.test.ts, src/delivery.ts | success: `pnpm vitest run src/delivery.test.ts && grep -q 'schedule_message' src/delivery.test.ts` | size: S
- [x] Build verification and config coherence: verify full build passes and both agent groups have provider:host in container.json (RED), run full build + all host tests + container tests + container typecheck, verify models.json has valid endpoint URLs for both groups (GREEN) | scope: groups/*/container.json, groups/*/models.json | success: `pnpm run build && pnpm vitest run && jq -e '.provider == "host"' groups/dm-with-wang/container.json && jq -e '.provider == "host"' groups/cli-with-wang/container.json` | size: S

</details>

<details>
<summary>Phase 11: Host-Mode Fragment Path Fix (3/3 done)</summary>

- [x] Host-mode path resolution in composeGroupClaudeMd: test composeGroupClaudeMd with provider:host in container.json produces fragment symlinks that resolve to readable files and .claude-shared.md resolves to container/CLAUDE.md (RED), detect provider from readContainerConfig, when provider=host use absolute host filesystem paths for all symlink targets — shared base to container/CLAUDE.md, skills to container/skills/<name>/instructions.md, modules to container/agent-runner/src/mcp-tools/<name>.instructions.md; when non-host keep /app/... paths unchanged (GREEN), extract host vs container path resolution to helper function (REFACTOR) | scope: src/claude-md-compose.ts, src/claude-md-compose.test.ts, src/container-config.ts | success: `pnpm vitest run src/claude-md-compose && grep -q 'resolveFragmentTarget\|hostMode' src/claude-md-compose.ts` | size: M
- [x] Fragment readability assertion for host-mode: test every symlink in .claude-fragments/ for a host-mode group resolves to a non-empty file (RED), add vitest test creating a group with provider:host calling composeGroupClaudeMd then iterating all entries in .claude-fragments/ verifying each symlink resolves to an existing file with content >0 bytes, verify inline fragments (soul.md) are non-empty (GREEN), extract symlink resolution checker to helper (REFACTOR) | scope: src/claude-md-compose.test.ts | success: `pnpm vitest run src/claude-md-compose` | size: S
- [x] Build verification and restart: verify full build + all tests + container typecheck pass (RED), run full build + all host tests + container typecheck, restart nanaclaw service (GREEN) | scope: src/claude-md-compose.ts | success: `pnpm run build && pnpm vitest run && pnpm exec tsc -p container/agent-runner/tsconfig.json --noEmit` | size: S

</details>

<details>
<summary>Phase 12: Worker Research Reliability (5/5 done)</summary>

- [x] Tool trace persistence: test TaskState with toolTrace field round-trips through JSON serialization, test executeToolCallingWorker result file includes toolTrace array with expected entries (RED), add optional `toolTrace?: ToolTraceEntry[]` to TaskState interface in contract.ts, import ToolTraceEntry type, write `loopResult.toolTrace` (capped: first 20 entries, result strings truncated to 500 chars) in executeToolCallingWorker in dispatch.ts, update formatTaskSummary in tools.ts to show trace summary with iteration count and tool names (GREEN) | scope: container/agent-runner/src/mcp-tools/local-worker/contract.ts, container/agent-runner/src/mcp-tools/local-worker/dispatch.ts, container/agent-runner/src/mcp-tools/local-worker/tools.ts, container/agent-runner/src/mcp-tools/local-worker/contract.test.ts, container/agent-runner/src/mcp-tools/local-worker/dispatch.test.ts | success: `cd container/agent-runner && bun test src/mcp-tools/local-worker/contract.test.ts && bun test src/mcp-tools/local-worker/dispatch.test.ts` | size: S
- [x] Configurable max_iterations: test TaskContract with max_iterations field validates correctly (positive value accepted), test without max_iterations validates for backward compat, test dispatch_worker MCP inputSchema includes max_iterations property (RED), add optional `max_iterations?: number` to TaskContract, update validateContract to accept positive max_iterations, add max_iterations to dispatch_worker inputSchema in index.ts, wire args.max_iterations through handleDispatchWorker, update executeToolCallingWorker to use `contract.max_iterations ?? 10` instead of hardcoded 6 (GREEN) | scope: container/agent-runner/src/mcp-tools/local-worker/contract.ts, container/agent-runner/src/mcp-tools/local-worker/contract.test.ts, container/agent-runner/src/mcp-tools/local-worker/dispatch.ts, container/agent-runner/src/mcp-tools/local-worker/dispatch.test.ts, container/agent-runner/src/mcp-tools/local-worker/index.ts | success: `cd container/agent-runner && bun test src/mcp-tools/local-worker/contract.test.ts && bun test src/mcp-tools/local-worker/dispatch.test.ts` | size: S
- [x] Iteration-aware research prompt: test buildWorkerPrompt with tools=['web_search','web_extract','wiki_write'] and type='research' includes decision-tree routing guidance mentioning wiki_write, test non-research type with tools does not include research-specific guidance, test prompt without tools is unchanged from baseline (RED), when contract has tools and type is 'research', append tool sequence decision tree to system prompt: "IF you have gathered search results AND extracted content THEN synthesize and call wiki_write next. Do NOT spend more than half your iterations on search/extract." (GREEN), extract tool budget guidance to named constant (REFACTOR) | scope: container/agent-runner/src/mcp-tools/local-worker/prompt-builder.ts, container/agent-runner/src/mcp-tools/local-worker/prompt-builder.test.ts | success: `cd container/agent-runner && bun test src/mcp-tools/local-worker/prompt-builder.test.ts` | size: M
- [x] SKILL.md iteration reconciliation: verify research-loop/SKILL.md contains outdated iteration limit language (RED), remove "≤6" limit from research-loop/SKILL.md, document max_iterations parameter with default 10, update dispatch template to include max_iterations: 10, update local-worker/SKILL.md to document max_iterations parameter in dispatch_worker and tool trace in worker results (GREEN) | scope: container/skills/research-loop/SKILL.md, container/skills/local-worker/SKILL.md | success: `! grep -q 'above 6' container/skills/research-loop/SKILL.md && grep -q 'max_iterations' container/skills/research-loop/SKILL.md && grep -q 'max_iterations' container/skills/local-worker/SKILL.md` | size: S
- [x] Build verification and regression: run full build + all host tests + all container tests + container typecheck (GREEN) | scope: container/agent-runner/src/mcp-tools/local-worker/** | success: `pnpm run build && pnpm vitest run && cd container/agent-runner && bun test && bun run typecheck` | size: S

</details>

<details>
<summary>Phase 13: Multi-Stage Research Pipeline (6/6 done)</summary>

- [x] Parallel inference capacity: verify current llama-cpp handles only 1 concurrent request (RED), investigate `--parallel N` flag on llama-cpp-server — test with 2 concurrent requests, if parallel slots work update startup command and models.json max_concurrent; if not, configure secondary Qwen3-8B instance on separate port with task_type routing for lighter tasks (GREEN) | scope: groups/*/models.json, .env | success: `curl -sf http://localhost:8080/v1/models > /dev/null && jq -e '.[0].max_concurrent >= 2' groups/dm-with-wang/models.json` | size: M
- [x] wiki_write raw tier with Hermes-style frontmatter: test wiki_write with tier='raw' writes to raw/articles/ subdirectory, test frontmatter includes source_url and ingested date, test sha256 computed from content body, test default behavior unchanged (RED), add source_url param to wiki_write inputSchema, when tier='raw' route to raw/articles/, compute sha256 via crypto module, build frontmatter with source_url + ingested + sha256 (GREEN), extract sha256 helper (REFACTOR) | scope: container/agent-runner/src/mcp-tools/wiki-write.ts, container/agent-runner/src/mcp-tools/wiki-write.test.ts | success: `cd container/agent-runner && bun test src/mcp-tools/wiki-write.test.ts` | size: M
- [x] Stage prompt templates: verify no prompt templates in research-loop skill (RED), create search-extract-prompt.md (8K context budget, one topic, tier: raw with source_url), summarize-prompt.md (raw truncated to 6K, wiki format with YAML/tags/cross-refs), review-prompt.md (scored 1-10 rubric, structured JSON {score, gaps, errors}) (GREEN) | scope: container/skills/research-loop/search-extract-prompt.md, container/skills/research-loop/summarize-prompt.md, container/skills/research-loop/review-prompt.md | success: `test -f container/skills/research-loop/search-extract-prompt.md && test -f container/skills/research-loop/summarize-prompt.md && test -f container/skills/research-loop/review-prompt.md && grep -q 'raw/articles' container/skills/research-loop/search-extract-prompt.md` | size: S
- [x] Research pipeline skill rewrite: verify current SKILL.md teaches autonomous agent loop (RED), rewrite with 5-stage orchestrated pipeline — Plan, Search+Write-Raw, Summarize, Review, Consolidate — including context budgets (8K/6K/4K), parallel dispatch guidance, coverage criteria, error handling (timeout → skip, malformed → retry once, max 1 retry per stage), reference prompt template companion files (GREEN) | scope: container/skills/research-loop/SKILL.md | success: `grep -q 'Stage 1' container/skills/research-loop/SKILL.md && grep -q 'raw/articles' container/skills/research-loop/SKILL.md && grep -q 'search-extract-prompt' container/skills/research-loop/SKILL.md && grep -q 'error\|timeout\|retry' container/skills/research-loop/SKILL.md` | size: M
- [x] Live test — research pipeline E2E: verify parallel inference, raw tier, and templates ready (RED), trigger research task through Nana using multi-stage pipeline, verify raw .md files in wiki raw/articles/ with sha256 frontmatter, verify summarized wiki-format entries, verify review JSON outputs (GREEN) | scope: container/skills/research-loop/SKILL.md | success: `find $HOME/private-knowledge/*/raw/articles -name '*.md' -newer /tmp/phase13-start 2>/dev/null | head -1 | xargs grep -q 'sha256:' && echo PASS` | size: M
- [x] Build verification and regression: run full build + all host tests + container tests + container typecheck (GREEN) | scope: container/agent-runner/src/mcp-tools/** | success: `pnpm run build && pnpm vitest run && cd container/agent-runner && bun test && bun run typecheck` | size: S

</details>

<details>
<summary>Phase 14: Unified Research Skill (3/5 done, 2 superseded)</summary>

- [x] Create research/ skill with plan gate: verify no research/ skill exists (RED), create SKILL.md (trigger patterns for ad-hoc and scheduled research, "When to Use" section) and instructions.md (plan gate requiring research-state.json before dispatch, 5-stage pipeline with state tracking, compaction recovery via state file read, knowledge wiki routing rules — all output to wiki not MEMORY.md, self-synthesis decision tree for analytical vs factual tasks), verify instructions.md is auto-injected by compose pipeline (GREEN) | scope: container/skills/research/SKILL.md, container/skills/research/instructions.md | success: `test -f container/skills/research/SKILL.md && test -f container/skills/research/instructions.md && grep -q 'research-state' container/skills/research/instructions.md && grep -q 'MEMORY' container/skills/research/instructions.md` | size: M
- [x] Move prompt templates and delete old skills: verify search-extract-prompt.md exists in research-loop/ (RED), move search-extract-prompt.md, summarize-prompt.md, review-prompt.md from research-loop/ to research/, delete deep-research/ and research-loop/ directories entirely, update or remove research-loop.test.ts in container agent-runner, verify compose pipeline handles missing skills gracefully (GREEN) | scope: container/skills/research/, container/skills/deep-research/, container/skills/research-loop/, container/agent-runner/src/mcp-tools/local-worker/research-loop.test.ts | success: `test -f container/skills/research/search-extract-prompt.md && ! test -d container/skills/deep-research && ! test -d container/skills/research-loop` | size: S
- [x] Update fragment composition and barrel references: grep codebase for 'deep-research' and 'research-loop' references (RED), update src/claude-md-compose.ts skill discovery if it hardcodes skill names, fix any broken imports or fragment references, verify no stale symlinks in group .claude-fragments/ (GREEN) | scope: src/claude-md-compose.ts, groups/*/.claude-fragments/ | success: `! grep -r 'deep-research\|research-loop' src/ container/agent-runner/src/ --include='*.ts' -l | grep -v test | grep -q . && pnpm run build` | size: S
- [blocked: design superseded by Phase 15 iterative pipeline] Live test — research via Telegram: restart nanoclaw, send research request via Telegram, verify agent writes research-state.json before dispatching, verify raw articles land in knowledge wiki with sha256, verify no MEMORY.md writes from research output (RED/GREEN manual) | scope: container/skills/research/ | success: manual verification — research-state.json created before dispatch, raw articles in wiki, MEMORY.md unchanged | size: M
- [blocked: design superseded by Phase 15 iterative pipeline] Build verification and regression: run full build + all host tests + container tests + container typecheck (GREEN) | scope: container/skills/research/**, container/agent-runner/src/** | success: `pnpm run build && pnpm vitest run && cd container/agent-runner && bun test && bun run typecheck` | size: S

</details>

<details>
<summary>Phase 15: Iterative Research Pipeline (8/8 done)</summary>

- [x] URL index builder: test buildUrlIndex scans fixture raw/articles/ frontmatter and produces newline-delimited .url-index with correct URLs, test loadAllUrlIndexes reads .url-index across multiple wiki paths from wikis.json and returns deduplicated Set, test missing .url-index returns empty set, test appendUrlIndex appends URL and is idempotent (RED), implement url-index.ts with buildUrlIndex(wikiPath) scanning raw/articles/*.md for source_url in frontmatter, loadAllUrlIndexes() reading raw/.url-index across all wikis from wikis.json, appendUrlIndex(wikiPath, url) with create-if-absent (GREEN), extract frontmatter parser to helper (REFACTOR) | scope: container/agent-runner/src/mcp-tools/url-index.ts, container/agent-runner/src/mcp-tools/url-index.test.ts | success: `cd container/agent-runner && bun test src/mcp-tools/url-index.test.ts` | size: M | data_contract: reads ~/.claude/wikis.json, reads <wiki>/raw/articles/*.md frontmatter, writes <wiki>/raw/.url-index
- [x] research_fetch MCP tool: test research_fetch with mock SearXNG returns raw article paths for new URLs, test dedup skips URLs in .url-index, test parallel fetch via Promise.allSettled handles partial failures gracefully (one URL 404 doesn't fail batch), test raw articles have correct Hermes frontmatter (sha256, source_url, ingested), test .url-index updated with new URLs after fetch, test empty SearXNG results returns {articles: [], skipped: 0} (RED), implement research-fetch.ts registered via registerTools() — import searchHandler (unwrap MCP envelope) and extractHandler logic, parallel fetch with Promise.allSettled, write raw articles reusing wiki-write buildFrontmatter+computeSha256, append to .url-index via url-index.ts (GREEN), extract SearXNG envelope unwrapper to helper (REFACTOR) | scope: container/agent-runner/src/mcp-tools/research-fetch.ts, container/agent-runner/src/mcp-tools/research-fetch.test.ts, container/agent-runner/src/mcp-tools/index.ts | success: `cd container/agent-runner && bun test src/mcp-tools/research-fetch.test.ts && bun run typecheck` | size: L | data_contract: reads SEARXNG_URL env + wikis.json, writes <wiki>/raw/articles/*.md + <wiki>/raw/.url-index
- [x] write_to contract extension and dispatch post-processing: test TaskContract with write_to field validates correctly (wiki + tier + title + tags), test without write_to validates for backward compat, test dispatch post-processing writes completed summarize worker output to <wiki>/episodic/<slug>.md with frontmatter when write_to.tier is 'episodic', test dispatch post-processing reads review JSON and updates status field in target article frontmatter when write_to.target_path is set, test post-processing skips when no write_to on contract, test malformed worker output with write_to logs warning and does not crash (RED), add optional write_to to TaskContract in contract.ts, add postProcessResult(state: TaskState) to dispatch.ts that checks write_to and routes output — episodic: write file with frontmatter, review: read target_path file update status in frontmatter, call postProcessResult after result file write in both executeSingleShotWorker and executeToolCallingWorker (GREEN), extract frontmatter update helper (REFACTOR) | scope: container/agent-runner/src/mcp-tools/local-worker/contract.ts, container/agent-runner/src/mcp-tools/local-worker/contract.test.ts, container/agent-runner/src/mcp-tools/local-worker/dispatch.ts, container/agent-runner/src/mcp-tools/local-worker/dispatch.test.ts | success: `cd container/agent-runner && bun test src/mcp-tools/local-worker/contract.test.ts && bun test src/mcp-tools/local-worker/dispatch.test.ts` | size: M
- [x] Rewrite research skill for iterative pipeline: verify current SKILL.md references old worker-driven search (RED), rewrite SKILL.md (~200 lines complex orchestration) — wiki-first (wiki_search existing coverage before query generation), iterative loop (generate queries → call research_fetch → evaluate results → generate more queries until satisfied), dispatch single-shot summarize workers with write_to episodic, dispatch single-shot review workers with write_to review targeting episodic path, Nana reviews failed articles, honest gap reporting, compaction recovery via research-state.json; rewrite instructions.md (~25 lines) — compact rules for iterative research; delete search-extract-prompt.md (GREEN) | scope: container/skills/research/SKILL.md, container/skills/research/instructions.md, container/skills/research/search-extract-prompt.md | success: `test -f container/skills/research/SKILL.md && ! test -f container/skills/research/search-extract-prompt.md && grep -q 'research_fetch' container/skills/research/SKILL.md && grep -q 'write_to' container/skills/research/SKILL.md && grep -q 'iterative\|loop\|more queries' container/skills/research/SKILL.md` | size: M
- [x] Update prompt templates for single-shot workers: verify summarize-prompt.md currently references tools (RED), rewrite summarize-prompt.md as single-shot template — input is raw article text as worker context, output is structured episodic markdown with sections (Summary, Key Findings, Sources), no tools field, dispatch_worker uses write_to to route output; rewrite review-prompt.md as single-shot — input is raw article + episodic summary as context, output is JSON {score, passed, issues}, dispatch_worker uses write_to with target_path to update status (GREEN) | scope: container/skills/research/summarize-prompt.md, container/skills/research/review-prompt.md | success: `! grep -q 'tools:' container/skills/research/summarize-prompt.md && grep -q 'write_to' container/skills/research/summarize-prompt.md && grep -q 'target_path' container/skills/research/review-prompt.md` | size: S
- [x] Live test — full pipeline via Telegram: restart nanoclaw with fresh session, send research request, verify Nana calls wiki_search first, verify research_fetch produces raw articles with .url-index updated, verify Nana iterates (generates follow-up queries if gaps found), verify summarize workers produce episodic articles via write_to post-processing, verify review workers update article status, verify Nana reports gaps honestly (RED/GREEN manual) | scope: container/skills/research/** | success: `find $HOME/private-knowledge/*/raw/articles -name '*.md' -newer /tmp/phase15-start 2>/dev/null | head -1 | xargs grep -q 'sha256:' && find $HOME/private-knowledge/*/raw/.url-index -newer /tmp/phase15-start 2>/dev/null | head -1 | xargs test -s && echo PASS` | size: M
- [x] Build verification and regression: run full build + all host tests + container tests + container typecheck (GREEN) | scope: container/agent-runner/src/** | success: `pnpm run build && pnpm vitest run && cd container/agent-runner && bun test && bun run typecheck` | size: S

</details>

<details>
<summary>Phase 16: Research Session Reliability (6/6 done)</summary>

- [x] Compact research_fetch output: test current handler returns per-article paths/quality/chars fields in response JSON, test response for 20 articles exceeds 2KB (RED), change return to {added, skipped, failed, new_articles: [{title, url}]} dropping paths/quality/chars, add test asserting JSON.stringify(result).length < 2048 for 20-article mock (GREEN), update SKILL.md example response block to match new compact shape (REFACTOR) | scope: container/agent-runner/src/mcp-tools/research-fetch.ts, container/agent-runner/src/mcp-tools/research-fetch.test.ts, container/skills/research/SKILL.md | success: `cd container/agent-runner && bun test src/mcp-tools/research-fetch.test.ts` | size: M
- [x] Compact research_summarize output: test current handler returns workers array with per-worker path/title/worker_id/episodic_path in response JSON (RED), return {dispatched, skipped, review_args} only — drop workers and skipped_details arrays (GREEN) | scope: container/agent-runner/src/mcp-tools/research-summarize.ts | success: `cd container/agent-runner && bun test` | size: S
- [x] Compact research_review output: test current handler returns workers and skipped_details arrays in response JSON (RED), return {dispatched, skipped} only — drop workers and skipped_details arrays (GREEN) | scope: container/agent-runner/src/mcp-tools/research-review.ts | success: `cd container/agent-runner && bun test` | size: S
- [x] Fix review source_url propagation: test writeEpisodicArticle produces episodic frontmatter WITHOUT source_url (RED), add optional source_url to WriteTo interface in contract.ts, have research_summarize extract source_url from raw article frontmatter via extractSourceUrl and pass in write_to, update writeEpisodicArticle in dispatch.ts to include source_url in episodic frontmatter when present (GREEN), verify findRawSource in research-review.ts now matches via fixture episodic with source_url (REFACTOR) | scope: container/agent-runner/src/mcp-tools/local-worker/contract.ts, container/agent-runner/src/mcp-tools/local-worker/contract.test.ts, container/agent-runner/src/mcp-tools/local-worker/dispatch.ts, container/agent-runner/src/mcp-tools/local-worker/dispatch.test.ts, container/agent-runner/src/mcp-tools/research-summarize.ts | success: `cd container/agent-runner && bun test src/mcp-tools/local-worker/contract.test.ts && bun test src/mcp-tools/local-worker/dispatch.test.ts` | size: M
- [x] Research skill communication + deep work awareness: verify SKILL.md lacks send_message guidance and instructions.md lacks progress update rule (RED), add progress update section to SKILL.md (call send_message every ~5 fetch rounds), add line to instructions.md ("Send the user a progress update every ~5 rounds during iterative research"), add deep work awareness section to SKILL.md (when deep work active keep researching until coverage saturated or deadline approaches — explore adjacent areas, don't stop after initial topics), update SKILL.md example response to match new compact output shape (GREEN) | scope: container/skills/research/SKILL.md, container/skills/research/instructions.md | success: `grep -q 'send_message' container/skills/research/SKILL.md && grep -q 'progress' container/skills/research/instructions.md && grep -q 'deep.work\|deadline' container/skills/research/SKILL.md && grep -q 'new_articles' container/skills/research/SKILL.md` | size: S
- [x] Build verification and regression: run full build + all host tests + container tests + container typecheck (GREEN) | scope: container/agent-runner/src/** | success: `pnpm run build && pnpm vitest run && cd container/agent-runner && bun test && bun run typecheck` | size: S

</details>

## Phase 17: Message Batching + Research Extraction Resilience

- [x] SearXNG engine diversification: verify karmasearch is enabled in current SearXNG config (RED), update data/searxng/settings.yml — keep `use_default_settings: true` and add selective engine overrides to disable karmasearch + karmasearch videos + karmasearch news (broken), enable bing + mojeek + qwant; restart SearXNG container with `docker restart searxng` (GREEN) | scope: data/searxng/settings.yml | success: `curl -sf "http://localhost:8888/config" 2>/dev/null | python3 -c "import sys,json; d=json.load(sys.stdin); web=['brave','bing','duckduckgo','google','mojeek','qwant','startpage']; enabled=[e['name'] for e in d['engines'] if e.get('enabled') and e['name'] in web]; karma=[e for e in d['engines'] if 'karma' in e['name'] and e.get('enabled')]; assert len(enabled)>=6, f'only {len(enabled)} web engines: {enabled}'; assert len(karma)==0, 'karmasearch still enabled'; print('PASS')"` | size: S
- [x] Poll loop pre-query accumulation window: test current poll loop starts query immediately after reading trigger messages with no delay (RED), add ACCUMULATION_WINDOW_MS=500 constant, after getPendingMessages returns messages with at least one trigger=1 in the idle-state outer loop, sleep ACCUMULATION_WINDOW_MS then re-read getPendingMessages and merge any new arrivals into the batch before proceeding to markProcessing and query — does NOT affect the existing push() mechanism which handles mid-query follow-ups independently; write test inserting two messages 200ms apart verifying both appear in the same processed batch (GREEN), extract constant to top of file (REFACTOR) | scope: container/agent-runner/src/poll-loop.ts, container/agent-runner/src/poll-loop.test.ts | success: `cd container/agent-runner && bun test` | size: M
- [x] Jina reader extraction helper: test tryJinaExtract returns extracted text from mock Jina r.jina.ai response, test returns null on fetch failure (graceful degradation), test returns null when JINA_READER_ENABLED=false, test returns null when Jina content is shorter than provided minLength threshold (RED), create container/agent-runner/src/mcp-tools/jina.ts with tryJinaExtract(url, maxChars, minLength?) — fetch `https://r.jina.ai/${url}` with Accept: text/plain header, return text if length > minLength else null, return null on any error; export from jina.ts (GREEN), extract JINA_READER_BASE constant (REFACTOR) | scope: container/agent-runner/src/mcp-tools/jina.ts, container/agent-runner/src/mcp-tools/jina.test.ts | success: `cd container/agent-runner && bun test src/mcp-tools/jina.test.ts` | size: M
- [x] Jina fallback in research-fetch: test fetchAndWrite uses Jina when Readability extraction is partial (<500 chars) and Jina returns longer content, test fetchAndWrite keeps Readability result when extraction is adequate (>500 chars), test fetchAndWrite keeps Readability result when Jina returns shorter content (RED), import tryJinaExtract from jina.ts, integrate into fetchAndWrite between Readability extraction and snippet fallback — when content.length < FULL_CONTENT_THRESHOLD, call tryJinaExtract(url, maxChars, content.length) sequentially (not parallel, respects Jina rate limit), use Jina result if non-null and longer (GREEN) | scope: container/agent-runner/src/mcp-tools/research-fetch.ts, container/agent-runner/src/mcp-tools/research-fetch.test.ts | success: `cd container/agent-runner && bun test src/mcp-tools/research-fetch.test.ts` | size: M
- [x] Jina fallback in web-extract: test extractHandler uses Jina when htmlToMarkdown produces <500 chars, test extractHandler keeps Readability result when adequate, test Jina failure falls through to existing behavior (RED), import tryJinaExtract from jina.ts, integrate into extractHandler — after htmlToMarkdown, if result.length < 500 and JINA_READER_ENABLED !== 'false', try Jina with minLength=result.length, use Jina result if non-null (GREEN) | scope: container/agent-runner/src/mcp-tools/web-extract.ts, container/agent-runner/src/mcp-tools/web-extract.test.ts | success: `cd container/agent-runner && bun test src/mcp-tools/web-extract.test.ts` | size: S
- [x] Build verification and regression: run full build + all host tests + all container tests + container typecheck (GREEN) | scope: container/agent-runner/src/** | success: `pnpm run build && pnpm vitest run && cd container/agent-runner && bun test && bun run typecheck` | size: S

## Phase 18: Knowledge Wiki + Project Management Agent Integration

- [x] wiki_read MCP tool: test wiki_read returns structured {title, tags, tier, created, body} from fixture wiki with tier→path mapping (articles: articles/**/<slug>.md recursive, raw: raw/articles/<slug>.md, episodic: episodic/<slug>.md, inbox: inbox/<slug>.md), test returns error for unknown slug or unknown wiki_name, test truncates body at max_chars (default 8000), test requires wiki_name parameter, test tier defaults to articles (RED), implement wiki-read.ts — resolve wiki by name from wikis.json, map tier to directory path, find slug recursively for articles tier, read file, parse frontmatter via new shared wiki-utils.ts (rich parser: title, tags, tier, created, status, source, source_url), truncate body, return structured JSON; register tool via registerTools() (GREEN), verify wiki-search.ts can adopt shared frontmatter parser from wiki-utils.ts (REFACTOR) | scope: container/agent-runner/src/mcp-tools/wiki-read.ts, container/agent-runner/src/mcp-tools/wiki-read.test.ts, container/agent-runner/src/mcp-tools/wiki-utils.ts | success: `cd container/agent-runner && bun test src/mcp-tools/wiki-read.test.ts` | size: M
- [x] wiki_stats MCP tool: test returns per-wiki counts for fixture wikis.json with fixture directory structure, test correct counts per tier (raw, episodic, inbox, articles), test optional wiki_name filters to single wiki, test graceful error when wikis.json missing (RED), implement wiki-stats.ts — load wikis.json, count .md files in raw/articles/, episodic/, inbox/, articles/ directories per wiki, return structured JSON {wikis: [{name, raw, episodic, inbox, articles}]}; register tool (GREEN) | scope: container/agent-runner/src/mcp-tools/wiki-stats.ts, container/agent-runner/src/mcp-tools/wiki-stats.test.ts | success: `cd container/agent-runner && bun test src/mcp-tools/wiki-stats.test.ts` | size: S
- [x] Wiki management container skill: verify no wiki-manager skill exists (RED), create SKILL.md with trigger patterns (wiki questions, knowledge queries, research follow-up), 4-tier lifecycle (raw → episodic → inbox → articles), decision tree (wiki_search for discovery when slug unknown → wiki_read for content when slug known), wiki_stats for health awareness, consolidation boundary ("episodic promotion is handled by the user — your job is research output and queries"), distinction from MEMORY.md (wiki = domain knowledge, memory = relationship/preference context); create instructions.md with compact rules for wiki tool usage (GREEN) | scope: container/skills/wiki-manager/SKILL.md, container/skills/wiki-manager/instructions.md | success: `test -f container/skills/wiki-manager/SKILL.md && test -f container/skills/wiki-manager/instructions.md && grep -q 'wiki_read' container/skills/wiki-manager/SKILL.md && grep -q 'wiki_search' container/skills/wiki-manager/SKILL.md && grep -q 'MEMORY' container/skills/wiki-manager/instructions.md` | size: M
- [x] Project management container skill: verify no project-manager skill exists (RED), create SKILL.md with trigger patterns (multi-step tasks, "plan this", "build X", complex requests spanning >1 turn), workflow (plan → break into tasks → execute in order → track state), .project/ directory convention in agent workspace CWD (plan.md for objective+approach+scope, tasks.md for ordered checklist with simplified schema `- [ ] Description | scope: <globs> | success: <criterion>`, state.md for current task+status+blockers+last_updated), session continuity (read .project/state.md on compaction recovery or session start), two modes (self-coding: implement directly; orchestrator: plan tasks, dispatch workers via dispatch_worker, review results), explicit boundary (.project/ is NOT memory — different persistence concern); create instructions.md with compact rules including when to create vs skip a project (GREEN) | scope: container/skills/project-manager/SKILL.md, container/skills/project-manager/instructions.md | success: `test -f container/skills/project-manager/SKILL.md && test -f container/skills/project-manager/instructions.md && grep -q '.project/' container/skills/project-manager/SKILL.md && grep -q 'tasks.md' container/skills/project-manager/SKILL.md && grep -q 'orchestrat' container/skills/project-manager/SKILL.md` | size: M
- [x] project_init MCP tool: test creates .project/ with plan.md, tasks.md, state.md in agent workspace CWD, test returns current state.md content if .project/ already exists (re-init guard — does not clobber), test plan.md contains name and objective from args, test state.md contains status: planning and current_task: none (RED), implement project-init.ts — resolve .project/ relative to process.cwd(), check existence (if exists return state), create directory + write template files with name+objective injected; register tool (GREEN) | scope: container/agent-runner/src/mcp-tools/project-init.ts, container/agent-runner/src/mcp-tools/project-init.test.ts | success: `cd container/agent-runner && bun test src/mcp-tools/project-init.test.ts` | size: M
- [x] Build verification and barrel wiring: verify wiki-read, wiki-stats, project-init imports NOT in mcp-tools barrel (RED), add imports to container/agent-runner/src/mcp-tools/index.ts, run full build + all host tests + all container tests + container typecheck (GREEN) | scope: container/agent-runner/src/mcp-tools/index.ts | success: `pnpm run build && pnpm vitest run && cd container/agent-runner && bun test && bun run typecheck` | size: S

## Phase 19: Worker Result Batching + Article Validation Gates

- [x] Worker result batch accumulation: test registerPendingBatch(5) causes checkWorkerResults to hold when <5 results ready, test checkWorkerResults releases all when count satisfied, test 60s timeout since first result releases partial batch, test non-batched workers (no batch registered) release immediately for backward compat (RED), add pendingBatches Map as module-state singleton to tools.ts — registerPendingBatch(count) stores {expected, completed:0, firstResult:null}, increment completed in checkWorkerResults per result picked up, gate return: if any batch has completed < expected AND <60s since firstResult hold and return null, release all when satisfied or timed out; export registerPendingBatch, wire into research_summarize (call after dispatch loop with count=dispatched.length) and research_review (same) (GREEN), extract batch tracker to batch-tracker.ts if tools.ts exceeds 200 lines (REFACTOR) | scope: container/agent-runner/src/mcp-tools/local-worker/tools.ts, container/agent-runner/src/mcp-tools/local-worker/tools.test.ts, container/agent-runner/src/mcp-tools/research-summarize.ts, container/agent-runner/src/mcp-tools/research-review.ts | success: `cd container/agent-runner && bun test src/mcp-tools/local-worker/tools.test.ts && bun test src/mcp-tools/local-worker/poll-integration.test.ts` | size: M
- [x] Article validation functions: test validateRawArticle returns {valid:true, issues:[]} for well-formed fixture, test returns {valid:false, issues} when source_url missing, sha256 missing, title empty, content <100 chars; test validateEpisodicArticle returns valid for well-formed fixture, test returns invalid when source_url missing, ## Summary absent, ## Key Points absent, tags empty, title empty (RED), implement article-validation.ts with shared ValidationResult type {valid:boolean, issues:string[]}, validateRawArticle(filePath) checking frontmatter (source_url, sha256, title non-empty) + content >100 chars, validateEpisodicArticle(filePath) checking frontmatter (source_url, title non-empty, tags non-empty) + sections (## Summary, ## Key Points) (GREEN) | scope: container/agent-runner/src/mcp-tools/article-validation.ts, container/agent-runner/src/mcp-tools/article-validation.test.ts | success: `cd container/agent-runner && bun test src/mcp-tools/article-validation.test.ts` | size: M
- [x] Raw article validation gate in research_fetch: test fetchAndWrite validates raw article after write and returns validation status in ArticleResult, test invalid raw article gets extraction: invalid added to frontmatter including articles with quality=partial that also fail validation (RED), import validateRawArticle from article-validation.ts, call after fs.writeFileSync in fetchAndWrite, if invalid update frontmatter with extraction: invalid via existing pattern, log warning, add validation_issues to ArticleResult (GREEN) | scope: container/agent-runner/src/mcp-tools/research-fetch.ts, container/agent-runner/src/mcp-tools/research-fetch.test.ts, container/agent-runner/src/mcp-tools/article-validation.ts | success: `cd container/agent-runner && bun test src/mcp-tools/research-fetch.test.ts` | size: S
- [x] Episodic article validation gate with auto-repair: test writeEpisodicArticle validates output, test auto-repairs source_url from writeTo.source_url when missing in written file, test auto-repairs tags from writeTo.tags when empty in written file, test adds status: needs-review for unfixable issues (missing ## Summary section), test checkWorkerResults includes validation failures for flagged articles (RED), import validateEpisodicArticle into dispatch.ts, call after writeEpisodicArticle; for fixable issues: re-read file, add source_url/tags from writeTo contract if available, rewrite file; for unfixable: add status: needs-review; add optional validationIssues field to TaskState, populate from validation result, include in checkWorkerResults output when present (GREEN) | scope: container/agent-runner/src/mcp-tools/local-worker/dispatch.ts, container/agent-runner/src/mcp-tools/local-worker/dispatch.test.ts, container/agent-runner/src/mcp-tools/local-worker/tools.ts, container/agent-runner/src/mcp-tools/article-validation.ts | success: `cd container/agent-runner && bun test src/mcp-tools/local-worker/dispatch.test.ts` | size: M
- [x] wiki_backfill_source_urls MCP tool: test scans fixture episodic dir and identifies entries missing source_url, test matches to raw articles by slug common-prefix overlap and writes source_url to episodic frontmatter, test skips entries already having source_url, test returns {fixed, unmatched, skipped} counts, test handles missing raw dir gracefully (RED), implement wiki-backfill.ts — for specified wiki_name, scan episodic/*.md for entries without source_url in frontmatter, for each build slug from filename, scan raw/articles/*.md for files with matching slug prefix (normalized), if match found extract source_url from raw frontmatter and write to episodic frontmatter using updateFrontmatterField pattern; register tool (GREEN) | scope: container/agent-runner/src/mcp-tools/wiki-backfill.ts, container/agent-runner/src/mcp-tools/wiki-backfill.test.ts | success: `cd container/agent-runner && bun test src/mcp-tools/wiki-backfill.test.ts` | size: M
- [x] Build verification and barrel wiring: verify article-validation and wiki-backfill imports NOT in mcp-tools barrel (RED), add imports to index.ts, run full build + all host tests + all container tests + container typecheck (GREEN) | scope: container/agent-runner/src/mcp-tools/index.ts | success: `pnpm run build && pnpm vitest run && cd container/agent-runner && bun test && bun run typecheck` | size: S

## Phase 20: Summarize Pipeline Reliability

- [x] Remove batch tracker from tools.ts and research_summarize: existing import of registerPendingBatch in tools.test.ts becomes compile error after export deletion (RED), remove PendingBatch interface, BATCH_SILENCE_MS, batchFilePath, readBatch, writeBatch, registerPendingBatch, _resetBatchForTest from tools.ts, remove all batch gating logic from checkWorkerResults (restore immediate-release for all results), remove registerPendingBatch import and call from research_summarize.ts only (keep in research_review.ts), clean up batch-related comments in poll-loop.ts (keep heartbeat-during-idle), update tools.test.ts — remove batch-specific tests, add test confirming immediate release with no batch file (GREEN), verify poll-integration.test.ts passes (REFACTOR) | scope: container/agent-runner/src/mcp-tools/local-worker/tools.ts, container/agent-runner/src/mcp-tools/local-worker/tools.test.ts, container/agent-runner/src/mcp-tools/research-summarize.ts, container/agent-runner/src/poll-loop.ts | success: `cd container/agent-runner && bun test src/mcp-tools/local-worker/tools.test.ts && bun test src/mcp-tools/local-worker/poll-integration.test.ts` | size: M
- [x] Stateful research_summarize with source_url dedup: test research_summarize with raw_dir+batch_size+wiki dispatches unsummarized articles from fixture raw dir, test skips articles whose source_url exists in fixture episodic dir, test writes summarize-state.json to AGENT_DIR with progress (total_raw, summarized, remaining), test reads existing summarize-state.json and resumes from offset, test old paths param still works (backward compat), test returns {dispatched, skipped_partial, skipped_existing, remaining}, test updates inputSchema with raw_dir+batch_size properties (RED), refactor summarizeHandler — add raw_dir+batch_size+wiki codepath: scan episodic dir building source_url Set via extractSourceUrl, list raw articles sorted, filter out covered+partial, dispatch batch_size, write summarize-state.json to path.join(AGENT_DIR, 'summarize-state.json'), update inputSchema adding raw_dir (string) + batch_size (number, default 60), keep paths codepath as fallback when raw_dir absent (GREEN), extract episodic source_url scanner to reusable helper (REFACTOR) | scope: container/agent-runner/src/mcp-tools/research-summarize.ts, container/agent-runner/src/mcp-tools/research-summarize.test.ts | success: `cd container/agent-runner && bun test src/mcp-tools/research-summarize.test.ts` | size: L
- [x] Research skill bulk summarization update: verify SKILL.md Summarize section teaches paths-array pattern (RED), rewrite Summarize section — replace paths-array example with raw_dir+batch_size pattern, add "Bulk Summarization" subsection teaching: tool handles offset+dedup via summarize-state.json, keep calling until remaining=0, don't message user per batch (progress every ~5 batches), on compaction recovery just call research_summarize again; update instructions.md adding "For bulk summarization use raw_dir param not paths" rule; verify old paths-array example is gone (GREEN) | scope: container/skills/research/SKILL.md, container/skills/research/instructions.md | success: `grep -q 'raw_dir' container/skills/research/SKILL.md && grep -q 'summarize-state' container/skills/research/SKILL.md && ! grep -q 'paths: \[list' container/skills/research/SKILL.md` | size: M
- [x] Stale task file cleanup: test checkWorkerResults deletes worker-tasks/ entry when matching result exists in completed set, test does NOT delete worker-tasks/ entry without matching completed result (RED), in checkWorkerResults after deleting completed result files, scan worker-tasks/ for files whose ID (filename minus .json) matches a just-completed result ID, delete those task files (GREEN) | scope: container/agent-runner/src/mcp-tools/local-worker/tools.ts, container/agent-runner/src/mcp-tools/local-worker/tools.test.ts | success: `cd container/agent-runner && bun test src/mcp-tools/local-worker/tools.test.ts` | size: S
- [x] Build verification and regression: verify no registerPendingBatch references in research-summarize.ts, verify no worker-batch.json references in codebase, run full build + all host tests + all container tests + container typecheck (RED/GREEN) | scope: container/agent-runner/src/** | success: `pnpm run build && pnpm vitest run && cd container/agent-runner && bun test && bun run typecheck && ! grep -rn 'registerPendingBatch' container/agent-runner/src/mcp-tools/research-summarize.ts` | size: S

<details>
<summary>Phase 21: Source Triage + Claim Extraction (6/6 done)</summary>

- [x] Source score function and domain allowlist: test computeSourceScore returns expected scores for known domains (fintrac.gc.ca=9, medium.com=3) and unknown (default 4), test depth scales with content length (log2-based 0-10), test loadAuthorityConfig reads fixture JSON, test returns default config when file missing (RED), implement source-score.ts with computeSourceScore(url, contentLength) using authority*0.6+depth*0.4, loadAuthorityConfig(wikiPath) reading <wiki>/source-authority.json with hardcoded fallback defaults (GREEN), seed source-authority.json for aml-wiki and trading-wiki on disk (REFACTOR) | scope: container/agent-runner/src/mcp-tools/source-score.ts, container/agent-runner/src/mcp-tools/source-score.test.ts | success: `cd container/agent-runner && bun test src/mcp-tools/source-score.test.ts` | size: M
- [x] Source score in research_fetch frontmatter: test buildRawFrontmatter includes source_score field, test fetchAndWrite computes score and passes to frontmatter, test score is number 0-10 (RED), import computeSourceScore+loadAuthorityConfig into research-fetch.ts, call in fetchAndWrite after content extraction, add source_score param to buildRawFrontmatter, write to frontmatter (GREEN) | scope: container/agent-runner/src/mcp-tools/research-fetch.ts, container/agent-runner/src/mcp-tools/research-fetch.test.ts | success: `cd container/agent-runner && bun test src/mcp-tools/research-fetch.test.ts` | size: S
- [x] Claim extraction in summarize prompt and dispatch: verify summarize-prompt.md lacks [CLAIM] guidance (RED), rewrite summarize-prompt.md adding ## Claims section with [CLAIM] tag format and AIDA quality rules (atomic, independent, declarative, source-attributable); update research-summarize.ts dispatchForPaths to add claim extraction boundary ("Extract atomic claims as [CLAIM] tags in a ## Claims section") and postcondition ({type:'contains', params:{substring:'## Claims'}}) to the worker contract; verify prompt stays within Qwen 4K-8K budget (GREEN) | scope: container/skills/research/summarize-prompt.md, container/agent-runner/src/mcp-tools/research-summarize.ts, container/agent-runner/src/mcp-tools/research-summarize.test.ts | success: `cd container/agent-runner && bun test src/mcp-tools/research-summarize.test.ts && grep -q 'CLAIM' container/skills/research/summarize-prompt.md` | size: M
- [x] Claim store and postProcessResult integration: test extractClaims parses [CLAIM] lines from markdown, test returns empty array when no claims, test appendClaims writes valid JSONL with claim+source_url+source_score+wiki+created, test creates claims.jsonl if not exists, test appends to existing file, test source_score defaults to 0 when not in raw frontmatter (pre-Phase-21 articles) (RED), implement claim-store.ts with extractClaims(text) and appendClaims(wikiPath, claims, metadata); call from postProcessResult in dispatch.ts after writeEpisodicArticle — extract claims, read source_score from write_to metadata or default 0, append to <wiki>/claims.jsonl (GREEN) | scope: container/agent-runner/src/mcp-tools/claim-store.ts, container/agent-runner/src/mcp-tools/claim-store.test.ts, container/agent-runner/src/mcp-tools/local-worker/dispatch.ts | success: `cd container/agent-runner && bun test src/mcp-tools/claim-store.test.ts` | size: M
- [x] Research skill claim documentation: verify SKILL.md lacks claim docs (RED), add claim extraction note to Summarize section — workers extract [CLAIM] tags automatically, claims stored in claims.jsonl per wiki, no agent action needed; update instructions.md noting automatic claim extraction (GREEN) | scope: container/skills/research/SKILL.md, container/skills/research/instructions.md | success: `grep -q 'claim' container/skills/research/SKILL.md && grep -q 'claims.jsonl' container/skills/research/SKILL.md` | size: S
- [x] Build verification and regression: run full build + all host tests + all container tests + container typecheck (RED/GREEN) | scope: container/agent-runner/src/** | success: `pnpm run build && pnpm vitest run && cd container/agent-runner && bun test && bun run typecheck` | size: S

</details>

<details>
<summary>Phase 22: Vector Claim Store (6/6 done)</summary>


- [x] claims_only mode on research_summarize: test that claims_only=true uses claim-extraction-only boundaries and write_to tier 'claims', test postProcessResult handles tier 'claims' by extracting claims and appending to claims.jsonl without writing episodic article (RED), extend WriteTo.tier union to include 'claims', update validateContract allowed tiers, add claims_only boolean param to summarizeHandler and inputSchema, modify dispatchForPaths to use simplified boundaries when claims_only (only ## Claims + [CLAIM] tags required, no ## Summary postcondition), set write_to.tier='claims', add 'claims' tier handler in postProcessResult (GREEN), extract boundary sets into named constants (REFACTOR) | scope: container/agent-runner/src/mcp-tools/research-summarize.ts, container/agent-runner/src/mcp-tools/research-summarize.test.ts, container/agent-runner/src/mcp-tools/local-worker/dispatch.ts, container/agent-runner/src/mcp-tools/local-worker/contract.ts | success: `cd container/agent-runner && bun test src/mcp-tools/research-summarize.test.ts && bun test src/mcp-tools/local-worker/dispatch.test.ts` | size: M
- [x] Embedding client for llama-server /embedding endpoint: test embedText returns Float32Array of 768 dimensions from mocked HTTP response, test embedBatch returns array of Float32Arrays, test connection error returns empty array gracefully (RED), implement claim-embeddings.ts with embedText(text): Promise<Float32Array> and embedBatch(texts): Promise<Float32Array[]> reading NANOCLAW_EMBED_URL from env (default http://localhost:8081/embedding), target model nomic-embed-text-v1.5 at 768 dims (GREEN) | scope: container/agent-runner/src/mcp-tools/claim-embeddings.ts, container/agent-runner/src/mcp-tools/claim-embeddings.test.ts | success: `cd container/agent-runner && bun test src/mcp-tools/claim-embeddings.test.ts` | size: S
- [x] SQLite vector store with cosine similarity: test insertClaim stores claim+embedding retrievable by id, test cosineSimilarity returns 1.0 for identical vectors and 0.0 for orthogonal, test searchSimilar returns ranked top-k results for a query embedding, test findDuplicates returns pairs above 0.92 threshold, test constructor accepts wikiPath and opens <wiki>/claims.db (RED), implement ClaimVectorStore class with bun:sqlite — schema claims(id INTEGER PRIMARY KEY, text TEXT, source_url TEXT, source_score REAL, wiki TEXT, created TEXT, embedding BLOB as Float32Array), constructor(wikiPath), methods insertClaim/searchSimilar(embedding, topK)/findDuplicates(threshold) with JS cosine similarity (GREEN), extract cosineSimilarity to standalone utility function (REFACTOR) | scope: container/agent-runner/src/mcp-tools/claim-vector-store.ts, container/agent-runner/src/mcp-tools/claim-vector-store.test.ts | success: `cd container/agent-runner && bun test src/mcp-tools/claim-vector-store.test.ts` | size: M
- [x] Embed pipeline (JSONL to claims.db): test pipeline reads JSONL entries and stores embedded claims in claims.db with correct embeddings, test incremental processing skips already-processed entries via line offset in embed-state.json, test dedup fallback checks claim text against existing DB entries when offset is inconsistent (RED), implement claim-embed-pipeline.ts with embedClaims(wikiPath) that reads claims.jsonl from last offset, embeds each claim via embedBatch from claim-embeddings client, stores in ClaimVectorStore, updates embed-state.json with processed line count — JSONL is append-only invariant from Phase 21 (GREEN) | scope: container/agent-runner/src/mcp-tools/claim-embed-pipeline.ts, container/agent-runner/src/mcp-tools/claim-embed-pipeline.test.ts | success: `cd container/agent-runner && bun test src/mcp-tools/claim-embed-pipeline.test.ts` | size: M
- [x] MCP tools — claim_embed, claim_search, claim_dedup: test claim_embed triggers pipeline and returns embedded count, test claim_search returns top-k results with metadata for query text, test claim_dedup returns duplicate pairs above threshold with source attribution, all tools accept wiki param to select correct claims.db (RED), implement claim-tools.ts with handlers — claim_embed (calls embedClaims for specified wiki), claim_search (embeds query via embedText, searches claims.db via searchSimilar, returns top-k with claim text + source_url + score), claim_dedup (calls findDuplicates, returns pairs with similarity score), register all three via registerTools(); add import './claim-tools.js' to index.ts barrel (GREEN) | scope: container/agent-runner/src/mcp-tools/claim-tools.ts, container/agent-runner/src/mcp-tools/claim-tools.test.ts, container/agent-runner/src/mcp-tools/index.ts | success: `cd container/agent-runner && bun test src/mcp-tools/claim-tools.test.ts` | size: M
- [x] Build + typecheck verification: run full host build, container typecheck, all container tests, verify no regressions in existing tests | scope: container/agent-runner/src/mcp-tools/** | success: `pnpm run build && pnpm exec tsc -p container/agent-runner/tsconfig.json --noEmit && cd container/agent-runner && bun test` | size: S

</details>

## Phase 23: Claim Backfill E2E

- [x] Embedding server setup: verify no process on port 8081 (RED), download nomic-embed-text-v1.5 GGUF model, launch second llama-server instance on port 8081 with --embedding flag, add NANOCLAW_EMBED_URL=http://localhost:8081/embedding to .env, verify 768-dim response (GREEN) | scope: .env | success: `curl -s http://localhost:8081/embedding -d '{"content":"test"}' -H 'Content-Type: application/json' | python3 -c "import sys,json; d=json.load(sys.stdin); assert len(d[0]['embedding'])==768"` | size: M
- [x] Research skill backfill workflow: verify SKILL.md has no ## Claim Backfill section (RED), add backfill workflow section documenting agent calls research_summarize with claims_only=true in a loop until remaining=0 then calls claim_embed to vectorize then claim_search/claim_dedup for validation, update instructions.md with backfill-specific rule (GREEN) | scope: container/skills/research/SKILL.md, container/skills/research/instructions.md | success: `grep -q '## Claim Backfill' container/skills/research/SKILL.md && grep -q 'claim_embed' container/skills/research/instructions.md` | size: S
- [x] Claims_only smoke test — AML wiki: run research_summarize with claims_only=true and batch_size=20 on aml-wiki raw/articles/, wait for workers to complete, verify claims.jsonl populated with valid entries containing claim/source_url/wiki fields (GREEN), fix any bugs surfaced during live test (REFACTOR) | scope: container/agent-runner/src/mcp-tools/** | success: `test -f /Users/jwang/private-knowledge/aml-wiki/claims.jsonl && wc -l < /Users/jwang/private-knowledge/aml-wiki/claims.jsonl | awk '{if($1>5) print "ok"}' | grep -q ok` | size: M
- [x] Embed pipeline E2E: run claim_embed for aml-wiki to vectorize claims.jsonl into claims.db, verify claims.db created with rows, run claim_search with a known AML topic query and verify non-empty results with similarity scores, run claim_dedup and verify response structure (GREEN), fix any bugs surfaced (REFACTOR) | scope: container/agent-runner/src/mcp-tools/** | success: `test -f /Users/jwang/private-knowledge/aml-wiki/claims.db && python3 -c "import sqlite3; c=sqlite3.connect('/Users/jwang/private-knowledge/aml-wiki/claims.db'); r=c.execute('SELECT count(*) FROM claims').fetchone()[0]; assert r>0, f'empty: {r}'"` | size: M
- [skipped: user deferred — AML wiki at 1,611 claims, trading wiki not started] Full claim backfill — both wikis: run research_summarize claims_only=true with raw_dir for aml-wiki and trading-wiki with batch_size=60, run claim_embed after batches complete, continue until both wikis have substantial claims.jsonl coverage (GREEN) | scope: container/agent-runner/src/mcp-tools/** | success: `test $(wc -l < /Users/jwang/private-knowledge/aml-wiki/claims.jsonl) -gt 500 && test $(wc -l < /Users/jwang/private-knowledge/trading-wiki/claims.jsonl) -gt 500` | size: M
- [x] Build + typecheck verification: run full host build, container typecheck, all container tests, verify no regressions from any fixes made during E2E | scope: container/agent-runner/src/mcp-tools/** | success: `cd /Users/jwang/nanaclaw && PATH="/Users/jwang/.hermes/node/bin:/Users/jwang/.hermes/node/lib/node_modules/corepack/shims:$PATH" pnpm -w run build && pnpm exec tsc -p container/agent-runner/tsconfig.json --noEmit && cd container/agent-runner && bun test` | size: S

## Phase 24: Deep Work Session Reliability

- [x] `finalizeExpiredDeepWork` + `calculateBackoffDelay` functions with tests: write tests asserting finalizeExpiredDeepWork returns summary when deadline passed and file exists, returns null when no file or deadline not passed, deletes state file, summary includes goal/steps/elapsed; calculateBackoffDelay returns 3000 at 0 errors, 6000 at 1, caps at 30000 (RED), implement both functions in deep-work.ts (GREEN), export for poll-loop use (REFACTOR) | scope: container/agent-runner/src/mcp-tools/deep-work.ts, container/agent-runner/src/mcp-tools/deep-work.test.ts | success: `cd /Users/jwang/nanaclaw/container/agent-runner && bun test src/mcp-tools/deep-work.test.ts` | size: M
- [x] Idle deep work re-entry + deadline finalization: verify idle branch has no deep work check (RED), add checkDeepWorkContinuation() to idle branch after worker results check guarded by lastRouting, add finalizeExpiredDeepWork() call in idle branch when continuation returns null and after the deep work while loop exits, write summary to messages_out when returned (GREEN) | scope: container/agent-runner/src/poll-loop.ts | success: `grep -c 'finalizeExpiredDeepWork' container/agent-runner/src/poll-loop.ts | awk '{if($1>=2) print "ok"}' | grep -q ok && grep -c 'checkDeepWorkContinuation' container/agent-runner/src/poll-loop.ts | awk '{if($1>=2) print "ok"}' | grep -q ok && cd container/agent-runner && bun run typecheck` | size: M
- [x] Retry with backoff in deep work while loop: verify catch block does immediate break (RED), replace with consecutive error counter using calculateBackoffDelay(), retry up to 3 times, send notification to messages_out with last error message after 3 failures, reset counter on success (GREEN) | scope: container/agent-runner/src/poll-loop.ts | success: `grep -q 'calculateBackoffDelay' container/agent-runner/src/poll-loop.ts && grep -q 'consecutiveErrors' container/agent-runner/src/poll-loop.ts && cd container/agent-runner && bun run typecheck` | size: M
- [x] Build + typecheck + full test verification: run host build, container typecheck, all container tests including new deep-work tests, verify no regressions | scope: container/agent-runner/src/** | success: `cd /Users/jwang/nanaclaw && PATH="/Users/jwang/.hermes/node/bin:/Users/jwang/.hermes/node/lib/node_modules/corepack/shims:$PATH" pnpm -w run build && pnpm exec tsc -p container/agent-runner/tsconfig.json --noEmit && cd container/agent-runner && bun test` | size: S

## Phase 25: Session Resume Guard + Entity Extraction Pipeline

- [x] Init timeout for processQuery: write test with mock provider whose async iterator never yields, verify processQuery aborts after timeout and throws (RED), race first event from async iterator against 90-second setTimeout, on timeout abort query and clear continuation directly (GREEN), extract INIT_TIMEOUT_MS constant (REFACTOR) | scope: container/agent-runner/src/poll-loop.ts, container/agent-runner/src/poll-loop.test.ts | success: `cd /Users/jwang/nanaclaw/container/agent-runner && bun test src/poll-loop.test.ts && bun run typecheck` | size: M
- [x] entity-store.ts — extractEntities + appendEntities with tests: write tests for extractEntities parsing [ENTITY type=PERSON] name|gender|age|profession|role|jurisdiction and ORGANIZATION/LOCATION/AMOUNT/CASE/DATE types, empty for no-match, malformed handling; appendEntities writes JSONL with dedup by type+lowercase(name)+source_url (RED), implement EntityEntry interface, extractEntities regex parser, appendEntities (GREEN) | scope: container/agent-runner/src/mcp-tools/entity-store.ts, container/agent-runner/src/mcp-tools/entity-store.test.ts | success: `cd /Users/jwang/nanaclaw/container/agent-runner && bun test src/mcp-tools/entity-store.test.ts` | size: M
- [x] Wire entity tier into dispatch.ts + contract.ts: add 'entities' to WriteTo tier union in contract.ts and update validator (RED), add entities case to dispatch.ts post-processing calling extractEntities + appendEntities (GREEN), write test verifying worker result with tier='entities' triggers entity extraction (REFACTOR) | scope: container/agent-runner/src/mcp-tools/local-worker/dispatch.ts, container/agent-runner/src/mcp-tools/local-worker/dispatch.test.ts, container/agent-runner/src/mcp-tools/local-worker/contract.ts | success: `cd /Users/jwang/nanaclaw/container/agent-runner && bun test src/mcp-tools/local-worker/dispatch.test.ts && bun run typecheck` | size: M
- [x] entities_only mode in research_summarize: verify no entities_only parameter exists (RED), add entities_only boolean with entity extraction prompt including precision filter, set write_to tier='entities', entity-specific postconditions (GREEN), write test verifying entities_only dispatches with correct tier and prompt (REFACTOR) | scope: container/agent-runner/src/mcp-tools/research-summarize.ts, container/agent-runner/src/mcp-tools/research-summarize.test.ts | success: `cd /Users/jwang/nanaclaw/container/agent-runner && bun test src/mcp-tools/research-summarize.test.ts && bun run typecheck` | size: M
- [x] Research skill docs update: add entity extraction workflow to SKILL.md documenting entities_only=true mode and entity types (PERSON with name/gender/age/profession/role/jurisdiction, ORGANIZATION, LOCATION, AMOUNT, CASE, DATE), update instructions.md with entity extraction guidance | scope: container/skills/research/SKILL.md, container/skills/research/instructions.md | success: `grep -q 'entities_only' container/skills/research/SKILL.md && grep -q 'ENTITY' container/skills/research/instructions.md` | size: S
- [x] Build + typecheck + full test verification: run host build, container typecheck, all container tests, verify no regressions | scope: container/agent-runner/src/** | success: `cd /Users/jwang/nanaclaw && PATH="/Users/jwang/.hermes/node/bin:/Users/jwang/.hermes/node/lib/node_modules/corepack/shims:$PATH" pnpm -w run build && pnpm exec tsc -p container/agent-runner/tsconfig.json --noEmit && cd container/agent-runner && bun test` | size: S

## Phase 26: Unified Knowledge Vector Store

- [x] vector-utils.ts + sentence-splitter.ts with tests: tests for cosineSimilarity with known vectors, sentenceSplit strips frontmatter/code blocks, extracts section headings, splits on .!? + whitespace, skips <10 chars, returns {text, section, position} (RED), implement vector-utils.ts extracting cosineSimilarity from claim-vector-store.ts, implement sentence-splitter.ts (GREEN) | scope: container/agent-runner/src/mcp-tools/vector-utils.ts, container/agent-runner/src/mcp-tools/vector-utils.test.ts, container/agent-runner/src/mcp-tools/sentence-splitter.ts, container/agent-runner/src/mcp-tools/sentence-splitter.test.ts | success: `cd /Users/jwang/nanaclaw/container/agent-runner && bun test src/mcp-tools/vector-utils.test.ts && bun test src/mcp-tools/sentence-splitter.test.ts` | size: M
- [x] KnowledgeVectorStore with tests: tests for knowledge.db creation, insertEntry with type='claim'/'sentence', chunked searchSimilar (10K rows/batch) returns correct top-K, type-filtered search, getAllByType (RED), implement knowledge-vector-store.ts generalizing ClaimVectorStore with type + contextual_text + article_slug + section columns, chunked search (GREEN) | scope: container/agent-runner/src/mcp-tools/knowledge-vector-store.ts, container/agent-runner/src/mcp-tools/knowledge-vector-store.test.ts | success: `cd /Users/jwang/nanaclaw/container/agent-runner && bun test src/mcp-tools/knowledge-vector-store.test.ts` | size: M
- [x] Refactor claim pipeline + migrate claims.db: existing claim tests as RED baseline, update claim-embed-pipeline.ts and claim-tools.ts to use KnowledgeVectorStore with type='claim', delete claim-vector-store.ts and fix importers, add migration reading existing claims.db rows into knowledge.db, keep claim-embeddings.ts and claim-store.ts as-is (GREEN) | scope: container/agent-runner/src/mcp-tools/claim-embed-pipeline.ts, container/agent-runner/src/mcp-tools/claim-tools.ts, container/agent-runner/src/mcp-tools/claim-vector-store.ts, container/agent-runner/src/mcp-tools/claim-vector-store.test.ts | success: `cd /Users/jwang/nanaclaw/container/agent-runner && bun test src/mcp-tools/claim-embed-pipeline.test.ts && bun test src/mcp-tools/claim-tools.test.ts && bun run typecheck` | size: M
- [x] Sentence embed pipeline with tests: tests for article processing into sentences with contextual prefix [title | section], is_claim via lowercase(trim(text)) match against claims.jsonl, chunked embedBatch at 256/request, state tracking in sentence-embed-state.json (RED), implement sentence-embed-pipeline.ts (GREEN) | scope: container/agent-runner/src/mcp-tools/sentence-embed-pipeline.ts, container/agent-runner/src/mcp-tools/sentence-embed-pipeline.test.ts | success: `cd /Users/jwang/nanaclaw/container/agent-runner && bun test src/mcp-tools/sentence-embed-pipeline.test.ts` | size: L
- [x] knowledge_search + knowledge_embed MCP tools + index.ts: implement knowledge_search (semantic search, optional type filter), knowledge_embed (embeds claims + sentences), keep claim_search/claim_embed/claim_dedup as aliases with type='claim' filter, update index.ts barrel, update SKILL.md + instructions.md (GREEN), write tests (REFACTOR) | scope: container/agent-runner/src/mcp-tools/knowledge-tools.ts, container/agent-runner/src/mcp-tools/knowledge-tools.test.ts, container/agent-runner/src/mcp-tools/index.ts, container/skills/research/SKILL.md, container/skills/research/instructions.md | success: `cd /Users/jwang/nanaclaw/container/agent-runner && bun test src/mcp-tools/knowledge-tools.test.ts && bun run typecheck && grep -q 'knowledge-tools' container/agent-runner/src/mcp-tools/index.ts` | size: M
- [x] Build + typecheck + full test verification | scope: container/agent-runner/src/** | success: `cd /Users/jwang/nanaclaw && PATH="/Users/jwang/.hermes/node/bin:/Users/jwang/.hermes/node/lib/node_modules/corepack/shims:$PATH" pnpm -w run build && pnpm exec tsc -p container/agent-runner/tsconfig.json --noEmit && cd container/agent-runner && bun test` | size: S

<details>
<summary>Phase 27: Conflict Detection + Claim Discovery (6/6 done)</summary>

- [x] KnowledgeVectorStore additions: tests for article_slug index existence (PRAGMA index_list), getByArticleSlug returning rows with embeddings filtered by slug + optional type (RED), add idx_knowledge_article index in constructor, implement getByArticleSlug method (GREEN) | scope: container/agent-runner/src/mcp-tools/knowledge-vector-store.ts, container/agent-runner/src/mcp-tools/knowledge-vector-store.test.ts | success: `cd /Users/jwang/nanaclaw/container/agent-runner && bun test src/mcp-tools/knowledge-vector-store.test.ts` | size: S
- [x] knowledge-conflicts.ts + tests: tests for findArticleConflicts returning cross-article pairs sorted by descending similarity excluding same-article matches with minSimilarity threshold, findQueryConflicts returning cross-article pairs from query results (RED), implement using store.getByArticleSlug for article sentences and chunked iteration for cross-article search (GREEN), extract shared pair-building logic (REFACTOR) | scope: container/agent-runner/src/mcp-tools/knowledge-conflicts.ts, container/agent-runner/src/mcp-tools/knowledge-conflicts.test.ts | success: `cd /Users/jwang/nanaclaw/container/agent-runner && bun test src/mcp-tools/knowledge-conflicts.test.ts` | size: M
- [x] knowledge-discovery.ts + tests: tests for discoverClaimsInArticle returning unclaimed sentences ranked by max similarity to any claim with nearest claim text and minSimilarity threshold (RED), implement loading article sentences via getByArticleSlug(slug, 'sentence') and all claims via db_allWithEmbeddings('claim'), computing pairwise cosine similarity (GREEN) | scope: container/agent-runner/src/mcp-tools/knowledge-discovery.ts, container/agent-runner/src/mcp-tools/knowledge-discovery.test.ts | success: `cd /Users/jwang/nanaclaw/container/agent-runner && bun test src/mcp-tools/knowledge-discovery.test.ts` | size: M
- [x] knowledge-classify.ts + tests: tests for classifyConflictPairs parsing [RESULT pair=N] tags into agree/contradict/unrelated with boundary validation rejecting malformed output, validateClaimCandidates parsing [VALIDATE N] tags into claim/not-claim, graceful fallback returning unclassified results when worker unavailable (RED), implement contract builders with decision-tree prompts, dispatch via executeAgentLoop, add deterministic output validator (GREEN) | scope: container/agent-runner/src/mcp-tools/knowledge-classify.ts, container/agent-runner/src/mcp-tools/knowledge-classify.test.ts | success: `cd /Users/jwang/nanaclaw/container/agent-runner && bun test src/mcp-tools/knowledge-classify.test.ts` | size: M
- [x] knowledge-analysis-tools.ts + index.ts: tests for knowledge_conflicts handler returning classified pairs and claim_discover handler returning validated candidates, tests for fallback to unclassified when worker unavailable (RED), implement knowledge_conflicts handler calling findArticleConflicts/findQueryConflicts then optionally classifyConflictPairs, implement claim_discover handler calling discoverClaimsInArticle then optionally validateClaimCandidates, register in knowledge-analysis-tools.ts, update index.ts barrel (GREEN) | scope: container/agent-runner/src/mcp-tools/knowledge-analysis-tools.ts, container/agent-runner/src/mcp-tools/knowledge-analysis-tools.test.ts, container/agent-runner/src/mcp-tools/index.ts | success: `cd /Users/jwang/nanaclaw/container/agent-runner && bun test src/mcp-tools/knowledge-analysis-tools.test.ts && grep -q 'knowledge-analysis-tools' container/agent-runner/src/mcp-tools/index.ts` | size: M
- [x] Build + typecheck + full test verification | scope: container/agent-runner/src/** | success: `cd /Users/jwang/nanaclaw && PATH="/Users/jwang/.hermes/node/bin:/Users/jwang/.hermes/node/lib/node_modules/corepack/shims:$PATH" pnpm -w run build && pnpm exec tsc -p container/agent-runner/tsconfig.json --noEmit && cd container/agent-runner && bun test` | size: S

</details>

<details>
<summary>Phase 28: Insight Extraction (6/6 done)</summary>

- [x] Insight extraction and storage: test extractInsights() extracts [INSIGHT] tags from markdown including mixed content, special characters, and empty lines; test appendInsights() creates insights.jsonl and appends with correct metadata (RED), add InsightEntry interface, INSIGHT_RE regex, extractInsights() and appendInsights() to claim-store.ts mirroring claim equivalents (GREEN) | scope: container/agent-runner/src/mcp-tools/claim-store.ts, container/agent-runner/src/mcp-tools/claim-store.test.ts | success: `cd /Users/jwang/nanaclaw/container/agent-runner && bun test src/mcp-tools/claim-store.test.ts` | size: S
- [x] insights_only mode in research_summarize: test that summarizeHandler with insights_only=true sets correct objective/boundaries/postconditions and tier='insights' (RED), add INSIGHTS_ONLY_OBJECTIVE, INSIGHTS_ONLY_BOUNDARIES, INSIGHTS_ONLY_POSTCONDITIONS constants and insights_only parameter with routing in dispatchForPaths and summarizeHandler (GREEN) | scope: container/agent-runner/src/mcp-tools/research-summarize.ts, container/agent-runner/src/mcp-tools/research-summarize.test.ts | success: `cd /Users/jwang/nanaclaw/container/agent-runner && bun test src/mcp-tools/research-summarize.test.ts` | size: S
- [x] Insight type in embedding pipeline and vector store: test that sentences matching insights.jsonl get type='insight', test claim takes priority over insight when sentence matches both (RED), widen KnowledgeInsert.type and PreparedSentence.type to include 'insight', add loadInsightTexts() to sentence-embed-pipeline.ts, update type classification with priority claim > insight > sentence (GREEN) | scope: container/agent-runner/src/mcp-tools/knowledge-vector-store.ts, container/agent-runner/src/mcp-tools/sentence-embed-pipeline.ts, container/agent-runner/src/mcp-tools/sentence-embed-pipeline.test.ts, container/agent-runner/src/mcp-tools/knowledge-vector-store.test.ts | success: `cd /Users/jwang/nanaclaw/container/agent-runner && bun test src/mcp-tools/sentence-embed-pipeline.test.ts && bun test src/mcp-tools/knowledge-vector-store.test.ts` | size: M
- [x] Insight type in knowledge search: test knowledge_search accepts type='insight' and filters results correctly (RED), update validType check in knowledge-tools.ts to accept 'insight', update tool description to list all three types (GREEN) | scope: container/agent-runner/src/mcp-tools/knowledge-tools.ts, container/agent-runner/src/mcp-tools/knowledge-tools.test.ts | success: `cd /Users/jwang/nanaclaw/container/agent-runner && bun test src/mcp-tools/knowledge-tools.test.ts` | size: S
- [x] Type-aware conflict detection: test knowledge_conflicts with type_filter='claim' excludes insight rows, test default behavior includes claims+sentences but excludes insight-vs-insight (RED), add optional type_filter parameter to findArticleConflicts and knowledge_conflicts MCP tool, filter loaded rows by type when specified (GREEN) | scope: container/agent-runner/src/mcp-tools/knowledge-conflicts.ts, container/agent-runner/src/mcp-tools/knowledge-conflicts.test.ts, container/agent-runner/src/mcp-tools/knowledge-analysis-tools.ts, container/agent-runner/src/mcp-tools/knowledge-analysis-tools.test.ts | success: `cd /Users/jwang/nanaclaw/container/agent-runner && bun test src/mcp-tools/knowledge-conflicts.test.ts && bun test src/mcp-tools/knowledge-analysis-tools.test.ts` | size: S
- [x] Build + typecheck + full test verification | scope: container/agent-runner/src/** | success: `cd /Users/jwang/nanaclaw && PATH="/Users/jwang/.hermes/node/bin:/Users/jwang/.hermes/node/lib/node_modules/corepack/shims:$PATH" pnpm -w run build && pnpm exec tsc -p container/agent-runner/tsconfig.json --noEmit && cd container/agent-runner && bun test` | size: S

</details>

<details>
<summary>Phase 29: Memory MCP Server — Core Storage + Tools (6/6 done)</summary>

- [x] Project scaffold and Pydantic models: create memory_server/ directory with __main__.py (entry point stub), models.py (MemoryEntry, SearchResult, ReinforcementEntry, StatsResponse Pydantic models matching schema spec), config.py (load from YAML or env vars: project_dir, global_dir, embedding config), requirements.txt (mcp>=1.0, pydantic>=2.0, pyyaml) | scope: memory_server/__main__.py, memory_server/models.py, memory_server/config.py, memory_server/requirements.txt | success: `cd memory_server && python -c "from models import MemoryEntry; from config import load_config; print('ok')"` | size: S
- [x] SQLite storage layer: implement storage.py with init_db() (create memories table with context column, reinforcements table, memories_fts virtual table, FTS5 sync triggers, meta table with schema_version), store() (insert with nanoid, created_at/updated_at, return MemoryEntry), get_by_id(), forget() (set active=0, superseded_by), tag() (add/remove from JSON array), stats() (counts by category, trust, active/superseded, oldest/newest) | scope: memory_server/storage.py | success: `cd memory_server && python -m pytest tests/test_storage.py -v` | size: M
- [x] Near-duplicate detection on store: implement exact-text duplicate check (lowercase strip match) — if duplicate found, call reinforce() (increment strength, add reinforcement row), return existing ID with action='reinforced'. If content within edit distance or high token overlap (>90%), return warning with candidate ID. Note: cosine-based dedup deferred to Phase 30 when embeddings are available | scope: memory_server/storage.py | success: `cd memory_server && python -m pytest tests/test_storage.py::test_dedup -v` | size: S
- [x] FTS5 keyword search: implement search_fts(query, limit, category, active_only) — query memories_fts with BM25 ranking, join with memories table for full fields, filter by category and active status, return list of SearchResult with score | scope: memory_server/storage.py | success: `cd memory_server && python -m pytest tests/test_search.py -v` | size: S
- [x] MCP server with core tools: implement server.py using mcp SDK — register memory_store, memory_search (FTS5-only), memory_forget, memory_tag, memory_stats as MCP tools with parameter schemas matching plan spec. Wire __main__.py to start server via stdio transport. Add scope parameter (project/global) routing to correct db path | scope: memory_server/server.py, memory_server/__main__.py | success: `cd memory_server && python -c "from server import create_server; print('ok')" && python -m pytest tests/ -v` | size: M
- [x] Integration test suite: create tests/fixtures/memories.json (20 entries across fact/preference/correction/entity categories with mixed trust levels), tests/test_integration.py — store all fixtures, search 5 different queries verifying relevant results appear in top 3, test reinforce-on-duplicate, test forget + active_only filter, test stats accuracy, test tag operations | scope: memory_server/tests/ | success: `cd memory_server && python -m pytest tests/test_integration.py -v` | size: M

</details>

<details><summary>Phase 30: Memory MCP Server — Embeddings + Claude Code (6/6 done)</summary>

- [x] Configurable embedding wrapper (embedding.py, local fastembed + HTTP server modes)
- [x] sqlite-vec integration and cosine search (vec0 virtual table, search_vec)
- [x] RRF fusion search (search_hybrid, cosine dedup, auto-embed on store)
- [x] Export and import tools (memory_export/memory_import, merge/replace modes)
- [x] Claude Code integration (memory-profile.md, memory-workflow.md)
- [x] Two-pass warm tier and E2E test (17 E2E tests, warm tier instructions)

</details>

## Phase 31: Memory MCP Server — Sidecar + Trust Lifecycle

- [x] Task 1: Sidecar HTTP client with graceful degradation: test verify_candidates returns filtered list when Qwen responds, test returns all candidates with verified=None when Qwen unavailable, test malformed Qwen response treated as fail-open (RED), implement sidecar.py with SidecarClient class using httpx to /v1/chat/completions, binary relevant/not-relevant per candidate, timeout and connection error handling (GREEN), extract prompt template to constant (REFACTOR) | scope: memory_server/sidecar.py, memory_server/tests/test_sidecar.py | success: `cd memory_server && python -m pytest tests/test_sidecar.py -v && python -c "from sidecar import SidecarClient"` | size: M
- [x] Task 2: Wire sidecar into MCP tools: test memory_verify tool returns verified results, test memory_search with verify=true filters via sidecar, test verify=true with Qwen offline returns unfiltered with warning, test malformed sidecar response is fail-open not crash (RED), add memory_verify tool and verify param to memory_search in server.py, pass SidecarClient through tool context (GREEN) | scope: memory_server/server.py, memory_server/tests/test_server.py | success: `cd memory_server && python -m pytest tests/test_server.py -v -k "verify"` | size: M
- [x] Task 3: Bidirectional contradiction tracking: test mark_contradiction updates contradicts JSON array on both entries, test idempotent (re-marking same pair is no-op), test nonexistent ID raises (RED), implement mark_contradiction in storage.py with bidirectional JSON array update, add memory_contradict tool in server.py (GREEN) | scope: memory_server/storage.py, memory_server/server.py, memory_server/tests/test_storage.py, memory_server/tests/test_server.py | success: `cd memory_server && python -m pytest tests/test_storage.py -v -k "contradict" && python -m pytest tests/test_server.py -v -k "contradict"` | size: M
- [x] Task 4: Transcript-to-memories extractor: test extract_memories returns list of proposed MemoryEntry from sample transcript, test empty transcript returns empty list, test Qwen unavailable returns empty list (RED), implement extractor.py with extract_memories function using httpx to sidecar endpoint, force trust=low and source=inferred on all output, validate with Pydantic (GREEN), extract shared HTTP helpers between sidecar.py and extractor.py if warranted (REFACTOR) | scope: memory_server/extractor.py, memory_server/tests/test_extractor.py | success: `cd memory_server && python -m pytest tests/test_extractor.py -v && python -c "from extractor import extract_memories"` | size: M
- [x] Task 5: Build + typecheck + full test verification: run full test suite to verify no regressions across all modules (RED=run existing tests first), fix any import or integration issues (GREEN) | scope: memory_server/**/*.py | success: `cd memory_server && python -m pytest tests/ -v --tb=short && python -c "from sidecar import SidecarClient; from extractor import extract_memories; from server import mcp"` | size: S
- [x] Task 6: Mark already-met exit criteria and verify remaining criteria coverage: verify auto-reinforce, trust tie-breaking, and supersede are working via existing tests (RED), update phase article exit criteria checkboxes for the 3 already-met criteria (GREEN) | scope: .dev-wiki/articles/phases/phase-31-memory-mcp-sidecar.md | success: `cd /Users/jwang/nanaclaw && python -c "c=open('.dev-wiki/articles/phases/phase-31-memory-mcp-sidecar.md').read().count('[x]'); assert c >= 3, f'only {c} checked'"` | size: S

## Phase 32: Memory MCP Server — Nanaclaw + Consolidation

- [x] Task 1: Global store + scope="all" fan-out search: test search_all queries both project and global DBs with independent RRF ranking, test project > global tie-breaking on equal scores, test scope="all" on memory_search MCP tool returns combined results, test memory_store/export/import support scope="global" (RED), implement search_all() in storage.py with dual-DB query + independent RRF ranking per DB + merge with project-preference tie-break, add scope="all" handling in server.py memory_search and existing tools (GREEN), extract DB path resolution into helper if duplicated (REFACTOR) | scope: memory_server/storage.py, memory_server/server.py, memory_server/tests/test_storage.py, memory_server/tests/test_server.py | success: `cd memory_server && uv run python -m pytest tests/test_storage.py -v -k "global" && uv run python -m pytest tests/test_server.py -v -k "global"` | size: M
- [x] Task 2: memory_prune tool: test prune returns candidates matching criteria (active=1 AND trust=low AND strength=1 AND (created_at older than 180 days OR access_count < 2 — access_count proxies for unreinforced since schema has no session_count)), test dry_run=True returns candidates without archiving, test execute mode sets active=0 on matches (RED), implement prune() in storage.py with criteria query, add memory_prune MCP tool with dry_run parameter in server.py (GREEN) | scope: memory_server/storage.py, memory_server/server.py, memory_server/tests/test_storage.py, memory_server/tests/test_server.py | success: `cd memory_server && uv run python -m pytest tests/ -v -k "prune"` | size: S
- [x] Task 3: Migration from MEMORY.md: test parse_memory_md extracts entries with correct type-to-category mapping (user→fact, feedback→correction, project→fact, reference→custom) and preserves original type as source-type tag, test migrate_file imports entries with source=imported and correct trust levels (feedback→high, others→medium), test idempotent re-migration via exact-text dedup, test original title preserved in context field (RED), implement migrate.py with parse_memory_md() and migrate_file() functions, add __main__ CLI entry point for `python -m memory_server.migrate --file <path>` (GREEN) | scope: memory_server/migrate.py, memory_server/tests/test_migration.py | success: `cd memory_server && uv run python -m pytest tests/test_migration.py -v && uv run python -m memory_server.migrate --help` | size: M
- [x] Task 4: Memory consolidation: test find_clusters groups memories by single-link cosine > 0.80 into clusters of 3+ (seed test memories with pre-computed embeddings), test consolidate generates merged entry via Qwen sidecar + supersedes originals with links, test Qwen unavailable skips cluster entirely (fail-closed not fail-open), test consolidated entry has source=consolidated and correct provenance (RED), implement consolidator.py with find_clusters() and consolidate() functions, add CONSOLIDATED to Source enum in models.py, add memory_consolidate MCP tool in server.py, add CLI entry point (GREEN) | scope: memory_server/consolidator.py, memory_server/models.py, memory_server/server.py, memory_server/tests/test_consolidation.py | success: `cd memory_server && uv run python -m pytest tests/test_consolidation.py -v && uv run python -c "from memory_server.consolidator import find_clusters, consolidate"` | size: M
- [x] Task 5: Nanaclaw MCP wiring + extraction CLI: test extract CLI reads transcript file and stores extracted memories to project DB, test empty transcript produces no entries (RED), add memory MCP server to groups/dm-with-wang/container.json mcpServers with MEMORY_PROJECT_DIR env for per-group DB isolation, implement extract CLI entry point in memory_server wrapping extractor.py with file I/O + storage calls (GREEN) | scope: groups/dm-with-wang/container.json, memory_server/extract_cli.py, memory_server/tests/test_extract_cli.py | success: `cd memory_server && uv run python -m pytest tests/test_extract_cli.py -v && python3 -c "import json; c=json.load(open('/Users/jwang/nanaclaw/groups/dm-with-wang/container.json')); assert 'memory' in c.get('mcpServers', {})"` | size: S
- [x] Task 6: Final integration + regression: run full test suite to verify no regressions across all modules including Phase 29-31 tests, verify global + project fan-out, prune dry-run, consolidation, migration round-trip work together (RED=run all tests), fix any integration issues (GREEN) | scope: memory_server/tests/ | success: `cd memory_server && uv run python -m pytest tests/ -v --tb=short` | size: S

## Phase 33: Claim Markers + Contextual Sentence Embeddings

- [x] Task 1: Create claim-spec.md: verify no claim-spec.md exists (RED), create canonical claim spec with claim ID format (clm_<nanoid-6>), [[clm_<id>]] inline marker syntax, frontmatter claims: array schema (id, sentence_ids, source_docs, verified_at, nli_score, jurisdiction, effective_from, effective_to), lifecycle (unlinked → linked → verified → stale, advisory-only until linker), explicit note that sentence_ids is always empty at write time (GREEN) | scope: /Users/jwang/knowledge-wiki/skills/knowledge-wiki/claim-spec.md | success: `test -f /Users/jwang/knowledge-wiki/skills/knowledge-wiki/claim-spec.md && grep -q 'clm_' /Users/jwang/knowledge-wiki/skills/knowledge-wiki/claim-spec.md && grep -q 'sentence_ids' /Users/jwang/knowledge-wiki/skills/knowledge-wiki/claim-spec.md && grep -q 'advisory' /Users/jwang/knowledge-wiki/skills/knowledge-wiki/claim-spec.md` | size: S
- [x] Task 2: Update article-conventions.md + reviewer-checklist.md: verify article-conventions.md has no "claims:" section and reviewer-checklist.md has 12 checks with "Score: N/12" (RED), add claims: frontmatter section to article-conventions.md documenting marker requirements by status (optional in draft, expected in reviewed, required in verified), add Check 13 (claims array matches inline markers) to reviewer-checklist.md, update output format "Score: N/12" → "Score: N/13" and verdict threshold "10-12" → "10-13" (GREEN) | scope: /Users/jwang/knowledge-wiki/skills/knowledge-wiki/article-conventions.md, /Users/jwang/knowledge-wiki/skills/knowledge-wiki/reviewer-checklist.md | success: `grep -q 'claims:' /Users/jwang/knowledge-wiki/skills/knowledge-wiki/article-conventions.md && grep -q '## Check 13' /Users/jwang/knowledge-wiki/skills/knowledge-wiki/reviewer-checklist.md && grep -q 'N/13' /Users/jwang/knowledge-wiki/skills/knowledge-wiki/reviewer-checklist.md` | size: S
- [x] Task 3: Update writer-prompt.md with claim marker instructions: verify writer-prompt.md doesn't mention "clm_" (RED), add claim marker section instructing writer to insert [[clm_<id>]] after factual statements, generate unique IDs (clm_<6-char-alphanumeric>), add claims: array to frontmatter with empty sentence_ids, do NOT add markers to transitional/opinion sentences or Related section (GREEN) | scope: /Users/jwang/knowledge-wiki/skills/wiki-absorb/writer-prompt.md | success: `grep -q 'clm_' /Users/jwang/knowledge-wiki/skills/wiki-absorb/writer-prompt.md && grep -q 'sentence_ids' /Users/jwang/knowledge-wiki/skills/wiki-absorb/writer-prompt.md` | size: S
- [x] Task 4: Add wiki-health checks 14 and 15: verify wiki-health SKILL.md describes 13 checks (RED), add check 14 "Claim Marker Integrity" (every [[clm_*]] in body has matching entry in claims: frontmatter and vice versa, severity ERROR for orphaned body markers, WARNING for orphaned frontmatter entries) and check 15 "Claim Coverage" (for reviewed/verified articles: % of non-trivial sentences with markers, non-trivial defined as >10 words excluding headers/list items/code blocks/Related section, severity WARNING below 50%), update "Run ALL 13 checks" to "Run ALL 15 checks" (GREEN) | scope: /Users/jwang/knowledge-wiki/skills/wiki-health/SKILL.md | success: `grep -q '### 14' /Users/jwang/knowledge-wiki/skills/wiki-health/SKILL.md && grep -q '### 15' /Users/jwang/knowledge-wiki/skills/wiki-health/SKILL.md && grep -q '15 checks' /Users/jwang/knowledge-wiki/skills/wiki-health/SKILL.md` | size: M
- [x] Task 5: Upgrade contextual prefix format: verify current code uses bracket format (RED), change prefix in sentence-embed-pipeline.ts from `[${title} | ${s.section}]` to natural language `Document: ${title}. Section: ${s.section}.` with fallback `Document: ${title}.` when no section (GREEN), extract prefix generation to function `buildContextPrefix(title: string, section: string | undefined): string` (REFACTOR) | scope: container/agent-runner/src/mcp-tools/sentence-embed-pipeline.ts | success: `grep -q 'Document:' container/agent-runner/src/mcp-tools/sentence-embed-pipeline.ts && grep -q 'buildContextPrefix' container/agent-runner/src/mcp-tools/sentence-embed-pipeline.ts` | size: S
- [x] Task 6: Return contextual_text in knowledge_search results: verify handleKnowledgeSearch result mapping does not include contextual_text (RED), add contextual_text: r.contextual_text to the results.map() object in handleKnowledgeSearch (GREEN) | scope: container/agent-runner/src/mcp-tools/knowledge-tools.ts | success: `grep -A10 'results.map' container/agent-runner/src/mcp-tools/knowledge-tools.ts | grep -q 'contextual_text'` | size: XS
- [x] Task 7: Install skills + create test article + re-embed + verify E2E: verify ~/.claude/skills/knowledge-wiki/claim-spec.md doesn't exist (RED), copy all modified files from /Users/jwang/knowledge-wiki/skills/ to ~/.claude/skills/ (claim-spec.md, article-conventions.md, writer-prompt.md, wiki-health/SKILL.md, reviewer-checklist.md), create a test article with 5+ claim markers in any wiki, re-embed at least 10 articles with new prefix, call knowledge_search and verify result contains "Document:" in contextual_text (GREEN) | scope: /Users/jwang/.claude/skills/**, container/agent-runner/src/mcp-tools/** | success: `test -f /Users/jwang/.claude/skills/knowledge-wiki/claim-spec.md && grep -q '### 14' /Users/jwang/.claude/skills/wiki-health/SKILL.md` | size: M

## Phase 34: Claim Linker

- [x] Task 1: Parse [[clm_*]] markers from article markdown: test parseClaimMarkers returns [] for article with no markers and [{id, text, section}] for fixture with 3+ markers including edge cases (marker at paragraph end, consecutive markers, markers across sections) (RED), implement parseClaimMarkers(content: string) using regex to find [[clm_<id>]] and extract preceding sentence text + current section heading (GREEN) | scope: container/agent-runner/src/mcp-tools/claim-linker.ts | success: `cd container/agent-runner && bun test src/mcp-tools/claim-linker.test.ts 2>&1 | grep -q 'parseClaimMarkers'` | size: S
- [x] Task 2: NLI entailment prompt builder + output parser: test buildNliPrompt produces numbered (claim, sentence) pair format, test parseNliResults extracts entails/neutral/contradicts labels and maps to numeric scores (ENTAILS=1.0, NEUTRAL=0.5, CONTRADICTS=0.0) from [NLI pair=N] format output (RED), implement buildNliPrompt(pairs) and parseNliResults(output) following knowledge-classify.ts regex pattern (GREEN) | scope: container/agent-runner/src/mcp-tools/claim-linker.ts | success: `cd container/agent-runner && bun test src/mcp-tools/claim-linker.test.ts 2>&1 | grep -q 'nli'` | size: S
- [x] Task 3: YAML frontmatter claims array update: test updateClaimsFrontmatter correctly updates sentence_ids, source_docs, nli_score, verified_at for matching claim IDs in fixture YAML while preserving unmodified claims and all other frontmatter fields (RED), implement updateClaimsFrontmatter(content, updates) with targeted regex/string parsing for claims array-of-objects in YAML frontmatter — parse claim blocks between `  - id:` boundaries, replace field values, write back (GREEN), extract claims array parser as reusable function (REFACTOR) | scope: container/agent-runner/src/mcp-tools/claim-linker.ts | success: `cd container/agent-runner && bun test src/mcp-tools/claim-linker.test.ts 2>&1 | grep -q 'updateClaimsFrontmatter'` | size: M
- [x] Task 4: Claim-sentence matching + NLI pipeline: test matchClaimToSentences returns candidates above threshold excluding same-article with mock embedding, test linkArticleClaims composes parse→match→NLI→update on fixture article with mock services (RED), implement matchClaimToSentences (embed claim text, over-fetch 2x top-K from searchSimilar, post-filter same-article, return above threshold), linkArticleClaims pipeline that reads article, parses markers, matches each claim, batches NLI pairs, computes nli_score (averaged numeric), derives source_docs from matched sentence source_url, calls updateClaimsFrontmatter (GREEN), extract similarity threshold (0.7) and top-K (10) as configurable defaults (REFACTOR) | scope: container/agent-runner/src/mcp-tools/claim-linker.ts | success: `cd container/agent-runner && bun test src/mcp-tools/claim-linker.test.ts 2>&1 | grep -q 'linkArticleClaims'` | size: M
- [x] Task 5: Register claim_link MCP tool: verify no claim_link tool exists in tool registration (RED), add claim_link tool definition with wiki (required), article_slug (required), skip_nli (optional bool), threshold (optional float), top_k (optional int) params, wire handleClaimLink handler that resolves wiki path, reads article file, runs linkArticleClaims, writes updated content back, returns {claims_processed, claims_linked, claims_verified, details}, add side-effect import in index.ts barrel if not already present (GREEN) | scope: container/agent-runner/src/mcp-tools/claim-linker.ts, container/agent-runner/src/mcp-tools/knowledge-tools.ts | success: `grep -q 'claim_link' container/agent-runner/src/mcp-tools/claim-linker.ts && cd container/agent-runner && bun run typecheck` | size: S
- [x] Task 6: E2E verification — test article with claim markers + linker: create a test article with 5+ claim markers in a wiki (satisfies Phase 33 remaining exit criterion), verify wiki-health check 14 (marker integrity) passes by checking all [[clm_*]] body markers have matching frontmatter entries programmatically (RED), run claim_link on test article with embedding server + Qwen running, verify ≥1 claim has non-empty sentence_ids and populated nli_score and source_docs (GREEN) | scope: container/agent-runner/src/mcp-tools/** | success: `cd container/agent-runner && bun test src/mcp-tools/claim-linker.test.ts 2>&1 | grep -q 'E2E'` | size: M

## Phase 35: Claim Conflict Detection

- [x] Task 1: Claim metadata parser from article frontmatter: test parseClaimMetadata extracts structured claim data (id, sentence_ids as number[], source_docs as string[], nli_score as number|null, verified_at as string|null) from fixture YAML frontmatter with multiple claims, test handles articles with no claims: array, test handles malformed/partial claim entries gracefully returning defaults (RED), implement parseClaimMetadata(content: string) building on existing parseClaimBlocks for block boundary detection then extracting field values from indented YAML lines within each block via regex (GREEN), export from claim-linker.ts for reuse by claim-conflicts.ts (REFACTOR) | scope: container/agent-runner/src/mcp-tools/claim-linker.ts, container/agent-runner/src/mcp-tools/claim-linker.test.ts | success: `cd container/agent-runner && bun test src/mcp-tools/claim-linker.test.ts 2>&1 | grep -q 'parseClaimMetadata'` | size: S
- [x] Task 2: Shared-evidence detector: test findSharedEvidence returns pairs of claims from different articles that share overlapping sentence_ids with overlap count and both claim IDs, test returns empty for claims with disjoint sentence_ids, test excludes claim pairs from the same article, test handles claims with empty sentence_ids gracefully (RED), implement findSharedEvidence(claims: ArticleClaims[]) where ArticleClaims = {articleSlug, claims: ClaimMeta[]} — computes pairwise sentence_id set intersections across articles, returns SharedEvidencePair[] sorted by overlap count descending (GREEN) | scope: container/agent-runner/src/mcp-tools/claim-conflicts.ts, container/agent-runner/src/mcp-tools/claim-conflicts.test.ts | success: `cd container/agent-runner && bun test src/mcp-tools/claim-conflicts.test.ts 2>&1 | grep -q 'findSharedEvidence'` | size: S
- [x] Task 3: Staleness and orphan detector: test detectStaleClaims flags claims where any source_docs file has mtime after verified_at date, test detects orphaned sentence_ids (IDs not found in knowledge.db), test returns empty for claims with all-fresh sources and valid sentence_ids, test skips claims with null verified_at (unverified claims cannot be stale) (RED), implement detectStaleClaims(claims: ClaimMeta[], store: KnowledgeVectorStore, wikiPath: string) that resolves source_docs to file paths in raw/ layer, compares file mtime against verified_at, queries knowledge.db for sentence_id existence, returns StalenessResult[] with stale_reason (evidence_updated | orphaned_ids) and details (GREEN) | scope: container/agent-runner/src/mcp-tools/claim-conflicts.ts, container/agent-runner/src/mcp-tools/claim-conflicts.test.ts | success: `cd container/agent-runner && bun test src/mcp-tools/claim-conflicts.test.ts 2>&1 | grep -q 'detectStaleClaims'` | size: S
- [x] Task 4: Cross-claim NLI conflict detector: test findClaimNliConflicts returns high-similarity claim pairs from different articles classified via mock NLI as agree/contradict/unrelated, test filters by cosine threshold (default 0.7), test excludes same-article pairs, test returns empty when no pairs exceed threshold (RED), implement findClaimNliConflicts(articleClaims: ArticleClaims[], embedFn, classifyFn, options) — collect all claims across articles, embed claim text on-the-fly via embedFn, compute pairwise cosine similarity for cross-article pairs above threshold, build ConflictPair objects compatible with classifyConflictPairs from knowledge-classify.ts, return classified results with claim metadata (GREEN), extract pairwise comparison into helper (REFACTOR) | scope: container/agent-runner/src/mcp-tools/claim-conflicts.ts, container/agent-runner/src/mcp-tools/claim-conflicts.test.ts | success: `cd container/agent-runner && bun test src/mcp-tools/claim-conflicts.test.ts 2>&1 | grep -q 'findClaimNliConflicts'` | size: M
- [x] Task 5: claim_conflicts MCP tool registration: test handleClaimConflicts returns structured report combining all three vectors for article-scoped mode, test wiki parameter required, test returns empty report when no claims exist, test conflict_type enum covers shared_evidence/stale/orphaned/nli_contradict/nli_agree (RED), implement claim_conflicts tool definition with wiki (required), article_slug (optional — omit to scan all articles with claims), top_k (default 20), threshold (default 0.7), skip_nli (optional bool) params. Handler reads article files, parses claim metadata via parseClaimMetadata, runs findSharedEvidence + detectStaleClaims + findClaimNliConflicts, merges into unified report. Register tool and add side-effect import in index.ts barrel (GREEN) | scope: container/agent-runner/src/mcp-tools/claim-conflicts.ts, container/agent-runner/src/mcp-tools/claim-conflicts.test.ts, container/agent-runner/src/mcp-tools/index.ts | success: `grep -q 'claim_conflicts' container/agent-runner/src/mcp-tools/claim-conflicts.ts && grep -q 'claim-conflicts' container/agent-runner/src/mcp-tools/index.ts && cd container/agent-runner && bun test src/mcp-tools/claim-conflicts.test.ts 2>&1 | grep -q 'handleClaimConflicts'` | size: M
- [x] Task 6: Build and regression verification: run full container test suite and typecheck, verify no regressions from baseline (534 tests), verify claim_conflicts tool is registered and callable | scope: container/agent-runner/src/mcp-tools/** | success: `cd container/agent-runner && bun test 2>&1 | grep -q '0 fail' && bun run typecheck` | size: S

## Phase 36: Claim Reconciliation

- [x] Task 1: reconcileArticle core function + unit tests: test reconcileArticle detects stale claims (source mtime > verified_at) and returns reconciled content with updated frontmatter, test orphaned claims (sentence_ids not in store) are detected and cleared then re-linked, test non-stale article returns unchanged content and empty report, test atomic behavior — no intermediate disk writes (clear + relink computed in memory) (RED), implement reconcileArticle(content, articleSlug, options: ReconcileOptions) — detect stale/orphan via StalenessCheckDeps → build ClaimUpdate[] clearing affected claims → call updateClaimsFrontmatter to produce cleared content → call linkArticleClaims on cleared content → return ReconcileResult with {staleClaims, orphanedClaims, relinkedCount, updatedContent, details} (GREEN), extract ReconcileResult type (REFACTOR) | scope: container/agent-runner/src/mcp-tools/claim-reconcile.ts, container/agent-runner/src/mcp-tools/claim-reconcile.test.ts | success: `cd container/agent-runner && bun test src/mcp-tools/claim-reconcile.test.ts 2>&1 | grep -q 'reconcileArticle'` | size: M
- [x] Task 2: MCP tool handler + dry_run mode + tests: test handleClaimReconcile resolves wiki path and finds articles with claims, test dry_run=true returns report without modifying file content (updatedContent matches original), test dry_run skips NLI (passes skip_nli=true to linkArticleClaims), test handler returns structured JSON report with per-article results (RED), implement handleClaimReconcile(args) MCP handler — resolve wiki path, scan article files for claim metadata, call reconcileArticle per article with any stale/orphaned claims, write updated content if not dry_run, return JSON with {articles_scanned, articles_reconciled, total_stale, total_orphaned, details[]}; register claim_reconcile tool with wiki (required), article_slug (optional), dry_run (optional bool, default false) params (GREEN) | scope: container/agent-runner/src/mcp-tools/claim-reconcile.ts, container/agent-runner/src/mcp-tools/claim-reconcile.test.ts | success: `cd container/agent-runner && bun test src/mcp-tools/claim-reconcile.test.ts 2>&1 | grep -q 'handleClaimReconcile'` | size: M
- [x] Task 3: Barrel import + build verification: verify claim-reconcile not yet imported in index.ts (RED), add `import './claim-reconcile.js'` to index.ts barrel, run full container test suite + typecheck, verify no regressions from 556-test baseline (GREEN) | scope: container/agent-runner/src/mcp-tools/index.ts | success: `grep -q 'claim-reconcile' container/agent-runner/src/mcp-tools/index.ts && cd container/agent-runner && bun test 2>&1 | grep -q '0 fail' && bun run typecheck` | size: S

## Phase 37: Small-to-Big Retrieval

- [x] Task 1: getWindow method on KnowledgeVectorStore + unit tests: test getWindow returns N sentences before and after a matched ID within the same article_slug ordered by id, test boundary cases (match near start/end of article returns fewer neighbors gracefully), test non-existent ID returns empty array, test with ID gaps in same article (non-contiguous IDs still return correct neighbors via LIMIT) (RED), implement getWindow(id, articleSlug, windowSize) on KnowledgeVectorStore — query N rows before (WHERE article_slug = $slug AND id < $matchId ORDER BY id DESC LIMIT $windowSize) + matched row + N rows after (WHERE article_slug = $slug AND id > $matchId ORDER BY id ASC LIMIT $windowSize), combine and sort by id, return KnowledgeRow[] (GREEN) | scope: container/agent-runner/src/mcp-tools/knowledge-vector-store.ts, container/agent-runner/src/mcp-tools/knowledge-vector-store.test.ts | success: `cd container/agent-runner && bun test src/mcp-tools/knowledge-vector-store.test.ts` | size: M
- [x] Task 2: expandSearchResults helper + overlap merge + wire into knowledge_search: test expandSearchResults with non-overlapping windows returns separate parent_text per result, test overlapping windows from same article (two matches 2 IDs apart with window_size=3) merge into single parent_text with highest similarity, test expand="none" returns results unchanged with no parent_text, test expand="window" adds parent_text field containing concatenated window sentence texts alongside original matched text (RED), implement expandSearchResults(results, store, windowSize) — for each SearchResult call store.getWindow, group by article_slug, detect overlapping ID ranges and merge into combined windows, build parent_text from concatenated window sentence texts; wire into handleKnowledgeSearch: add expand (string, default "none") and window_size (number, default 3) params to tool schema, call expandSearchResults when expand="window" (GREEN), extract ExpandedSearchResult type (REFACTOR) | scope: container/agent-runner/src/mcp-tools/knowledge-tools.ts, container/agent-runner/src/mcp-tools/knowledge-tools.test.ts | success: `cd container/agent-runner && bun test src/mcp-tools/knowledge-tools.test.ts` | size: M
- [x] Task 3: Build verification + regression check: verify 566-test baseline still passes (RED), run full container test suite + typecheck, verify expand param appears in knowledge_search tool inputSchema, confirm no regressions (GREEN) | scope: container/agent-runner/src/mcp-tools/ | success: `cd container/agent-runner && bun test 2>&1 | tail -1 | grep -E '^[0-9]+ pass' && bun test 2>&1 | grep -v '0 fail' | grep -qv 'fail' && bun run typecheck` | size: S

## Phase 38: Review Remediation

- [x] Task 1: Search routing guidance: verify no knowledge-routing.md exists (RED), create container/skills/wiki-manager/knowledge-routing.md with intent-to-tool mapping covering knowledge_search, claim_search, wiki_search, claim_link, claim_conflicts, claim_reconcile, wiki_stats, knowledge_embed, knowledge_conflicts; update instructions.md to reference it and add claim tools to decision tree (GREEN) | scope: container/skills/wiki-manager/knowledge-routing.md, container/skills/wiki-manager/instructions.md | success: `test -f container/skills/wiki-manager/knowledge-routing.md && grep -q 'claim_link' container/skills/wiki-manager/instructions.md` | size: S
- [x] Task 2: resolveWikiPath + text() in wiki-utils.ts + tests: test resolveWikiPath returns path for known wiki and null for unknown, test text() returns CallToolResult with text content type (RED), add resolveWikiPath(wikiName) to wiki-utils.ts wrapping loadWikis + resolveWikiByName + fallback-to-first + return path, add text(msg) returning CallToolResult, export both (GREEN) | scope: container/agent-runner/src/mcp-tools/wiki-utils.ts, container/agent-runner/src/mcp-tools/wiki-utils.test.ts | success: `cd container/agent-runner && bun test src/mcp-tools/wiki-utils.test.ts` | size: M
- [x] Task 3: Replace duplicates in drop-in files: grep confirms private loadWikis/resolveWikiPath/text in target files (RED), replace with imports from wiki-utils.ts in claim-tools.ts, claim-linker.ts, claim-conflicts.ts, claim-reconcile.ts, knowledge-tools.ts, knowledge-analysis-tools.ts (all 3 functions); url-index.ts, local-worker/dispatch.ts (loadWikis+WikiEntry only — no resolveWikiPath/text in these). Remove private copies. Verify tests pass per file (GREEN) | scope: container/agent-runner/src/mcp-tools/claim-tools.ts, claim-linker.ts, claim-conflicts.ts, claim-reconcile.ts, knowledge-tools.ts, knowledge-analysis-tools.ts, url-index.ts, local-worker/dispatch.ts | success: `cd container/agent-runner && bun test 2>&1 | grep -E '0 fail'` | size: M
- [x] Task 4: Replace duplicates in adaptation files: grep confirms private loadWikis in wiki-search.ts, research-fetch.ts, wiki-write.ts, research-summarize.ts (RED), replace loadWikis+WikiEntry with import from wiki-utils.ts in each file — these do custom wiki resolution so only loadWikis is replaced, not resolveWikiPath. Remove private copies (GREEN) | scope: container/agent-runner/src/mcp-tools/wiki-search.ts, research-fetch.ts, wiki-write.ts, research-summarize.ts | success: `cd container/agent-runner && bun test 2>&1 | grep -E '0 fail'` | size: S
- [x] Task 5: Wiki-manager taxonomy alignment: verify instructions.md uses inconsistent tier terminology (RED), update to content-model terms — raw (immutable pipeline input), episodic (research findings, append-only), articles with lifecycle status (draft/reviewed/verified/stale). Preserve wiki_read tier param references (GREEN) | scope: container/skills/wiki-manager/instructions.md | success: `grep -q 'pipeline input' container/skills/wiki-manager/instructions.md && grep -q 'lifecycle' container/skills/wiki-manager/instructions.md` | size: S
- [x] Task 6: Context builder dual-read + tests: test generateMemoryFragment includes entries from memories table when present alongside MEMORY.md entries, test falls back to MEMORY.md-only when memories table absent (FTS-only db), test dedup by exact title match produces single entry (RED), in context-builder.ts after reading MEMORY.md entries check if memory.db has memories table (SELECT 1 FROM sqlite_master WHERE type='table' AND name='memories'), if yes query active=1 ORDER BY created_at DESC, convert to MemoryEntry (map category to type, first line as title), merge with MEMORY.md entries, dedup by title, select within TOKEN_BUDGET (GREEN), extract readMcpMemories helper (REFACTOR) | scope: src/modules/memory/context-builder.ts, src/modules/memory/context-builder.test.ts | success: `pnpm vitest run src/modules/memory/context-builder.test.ts` | size: M
- [x] Task 7: Build verification + regression: verify 578-test baseline (RED), run full host test suite + container test suite + both typechecks, confirm no regressions (GREEN) | scope: src/**, container/agent-runner/src/** | success: `pnpm run build && pnpm vitest run && cd container/agent-runner && bun test 2>&1 | grep -E '0 fail' && bun run typecheck` | size: S

## Phase 40: Memory Migration + Claim Dedup Guard

- [x] Task 1: Auto-migrate MEMORY.md at spawn + tests: test migrateMemoryMdToDb creates memories table and inserts entries with correct category mapping (user→fact, feedback→correction, project→fact, reference→custom) and trust (feedback→high, others→medium), test idempotency — second call with same entries inserts nothing new (exact-content dedup), test empty MEMORY.md produces no inserts (RED), implement migrateMemoryMdToDb(entries, dbPath) in context-builder.ts — open memory.db with write access, CREATE TABLE IF NOT EXISTS memories matching MCP server schema exactly (id TEXT PRIMARY KEY, content, context, category, trust, strength, source, source_session, tags, active, superseded_by, contradicts, embedding, created_at, updated_at, access_count), for each entry check duplicate (SELECT 1 FROM memories WHERE content=$content AND active=1), insert new with crypto.randomUUID() id and mapped category/trust, close DB. Call from generateMemoryFragment before dual-read (GREEN), extract TYPE_TO_CATEGORY and TYPE_TO_TRUST constants (REFACTOR) | scope: src/modules/memory/context-builder.ts, src/modules/memory/context-builder.test.ts | success: `pnpm vitest run src/modules/memory/context-builder.test.ts` | size: M
- [x] Task 2: claim_dedup max_claims guard + duration: test claim_dedup returns error when claim count exceeds max_claims param, test duration_ms present in successful response (RED), in claim-tools.ts handleClaimDedup add max_claims param (default 1000), run COUNT query on knowledge table WHERE type='claim' before db_allWithEmbeddings — if count > maxClaims return error with count and suggestion to reduce threshold or split input, add timing (Date.now before/after loop) and include duration_ms in response JSON (GREEN) | scope: container/agent-runner/src/mcp-tools/claim-tools.ts, container/agent-runner/src/mcp-tools/claim-tools.test.ts | success: `cd container/agent-runner && bun test src/mcp-tools/claim-tools.test.ts` | size: S
- [x] Task 3: Build verification + regression: run host tests + container mcp-tools tests + both typechecks, confirm no regressions (RED/GREEN) | scope: src/**, container/agent-runner/src/** | success: `pnpm run build && pnpm vitest run && cd container/agent-runner && bun test src/mcp-tools/ 2>&1 | grep -E '0 fail' && bun run typecheck` | size: S

## Phase 42: Pipeline Simplification

- [x] Task 1: Delete old pipeline tool files + test files + update barrel imports: verify files exist (RED), delete research-summarize.ts, research-summarize.test.ts, research-review.ts, claim-tools.ts, claim-tools.test.ts, claim-linker.ts, claim-linker.test.ts, claim-conflicts.ts, claim-conflicts.test.ts, claim-reconcile.ts, claim-reconcile.test.ts, claim-embed-pipeline.ts, claim-embed-pipeline.test.ts, knowledge-discovery.ts, knowledge-discovery.test.ts, claim-vector-store.test.ts; remove 6 barrel imports from index.ts (research-summarize, research-review, claim-tools, claim-linker, claim-conflicts, claim-reconcile) (GREEN) | scope: container/agent-runner/src/mcp-tools/*.ts, container/agent-runner/src/mcp-tools/index.ts | success: `! test -f container/agent-runner/src/mcp-tools/research-summarize.ts && ! test -f container/agent-runner/src/mcp-tools/claim-tools.ts && ! test -f container/agent-runner/src/mcp-tools/claim-linker.ts && ! test -f container/agent-runner/src/mcp-tools/claim-embed-pipeline.ts && ! test -f container/agent-runner/src/mcp-tools/knowledge-discovery.ts && ! grep -q 'research-summarize' container/agent-runner/src/mcp-tools/index.ts && ! grep -q 'claim-tools' container/agent-runner/src/mcp-tools/index.ts` | size: M
- [x] Task 2: Remove claim_discover from knowledge-analysis-tools.ts + clean knowledge-classify.ts: verify claim_discover handler and knowledge_conflicts handler both exist (RED), remove handleClaimDiscover function + claim_discover tool registration + discoverClaimsInArticle import + validateClaimCandidates import from knowledge-analysis-tools.ts; remove ClaimCandidate import, validateClaimCandidates, buildClaimValidationPrompt, parseClaimValidation, validateClaimOutput, ClaimValidationResult, ValidatedClaimCandidate from knowledge-classify.ts; keep conflict classification functions intact (GREEN) | scope: container/agent-runner/src/mcp-tools/knowledge-analysis-tools.ts, container/agent-runner/src/mcp-tools/knowledge-classify.ts | success: `! grep -q 'claim_discover' container/agent-runner/src/mcp-tools/knowledge-analysis-tools.ts && grep -q 'knowledge_conflicts' container/agent-runner/src/mcp-tools/knowledge-analysis-tools.ts && ! grep -q 'knowledge-discovery' container/agent-runner/src/mcp-tools/knowledge-classify.ts && grep -q 'classifyConflictPairs' container/agent-runner/src/mcp-tools/knowledge-classify.ts` | size: S
- [x] Task 3: Strip dispatch.ts extraction branches + delete claim-store.ts + entity-store.ts + narrow contract.ts + inline types: verify dispatch.ts imports from claim-store and entity-store (RED), remove extractClaims/appendClaims/extractInsights/appendInsights/extractEntities/appendEntities imports and all usage branches from dispatch.ts; narrow WriteTo.tier in contract.ts from 5 to 2 values ('episodic' or 'review') + update validator; inline ClaimEntry and InsightEntry type interfaces into sentence-embed-pipeline.ts; delete claim-store.ts, claim-store.test.ts, entity-store.ts, entity-store.test.ts (GREEN) | scope: container/agent-runner/src/mcp-tools/local-worker/dispatch.ts, container/agent-runner/src/mcp-tools/local-worker/contract.ts, container/agent-runner/src/mcp-tools/sentence-embed-pipeline.ts, container/agent-runner/src/mcp-tools/claim-store.ts, container/agent-runner/src/mcp-tools/entity-store.ts | success: `! test -f container/agent-runner/src/mcp-tools/claim-store.ts && ! test -f container/agent-runner/src/mcp-tools/entity-store.ts && ! grep -q 'claim-store' container/agent-runner/src/mcp-tools/local-worker/dispatch.ts && grep -q 'ClaimEntry' container/agent-runner/src/mcp-tools/sentence-embed-pipeline.ts && grep -q "'episodic' | 'review'" container/agent-runner/src/mcp-tools/local-worker/contract.ts` | size: M
- [x] Task 4: Update skill documentation + .claude-fragments: verify SKILL.md references deleted tools (RED), update container/skills/research/SKILL.md + instructions.md — remove summarize/review/claim references; update container/skills/wiki-manager/knowledge-routing.md — remove claim tool routing; delete container/skills/research/summarize-prompt.md + review-prompt.md; update groups/dm-with-wang/.claude-fragments/skill-research.md + skill-wiki-manager.md — remove deleted tool references (GREEN) | scope: container/skills/research/*, container/skills/wiki-manager/*, groups/dm-with-wang/.claude-fragments/skill-research.md, groups/dm-with-wang/.claude-fragments/skill-wiki-manager.md | success: `! grep -q 'research_summarize' container/skills/research/SKILL.md && ! grep -q 'claim_link' container/skills/wiki-manager/knowledge-routing.md && ! test -f container/skills/research/summarize-prompt.md && ! grep -q 'research_summarize' groups/dm-with-wang/.claude-fragments/skill-research.md` | size: S
- [x] Task 5: Clear stale wiki data across all wikis: check for stale data existence (RED), delete episodic/ + articles/ + inbox/ dirs and claims.jsonl + insights.jsonl + entities.jsonl + claim-discovery-*.json + embed-state.json + sentence-embed-state.json + source-authority.json across all wikis under /Users/jwang/private-knowledge/; keep raw/ + knowledge.db + index.md + schema.md + log.md (GREEN) | scope: /Users/jwang/private-knowledge/*/ | success: `! test -d /Users/jwang/private-knowledge/aml-wiki/episodic && ! test -f /Users/jwang/private-knowledge/aml-wiki/claims.jsonl && ! test -d /Users/jwang/private-knowledge/agentic-engineering-wiki/articles` | size: S | data_contract: reads /Users/jwang/private-knowledge/*/episodic/, articles/, inbox/, claims.jsonl, insights.jsonl, entities.jsonl; writes (deletes) same
- [x] Task 6: Full test verification — container + host: run container test suite + host test suite + both typechecks, verify no regressions beyond deleted test files (RED/GREEN) | scope: container/agent-runner/src/**, src/** | success: `cd container/agent-runner && bun test 2>&1 | grep -E '0 fail' && bun run typecheck && cd ../.. && pnpm run build && pnpm vitest run` | size: S

<details>
<summary>Phase 43: Knowledge Pipeline Instruction Cleanup (6/6 done)</summary>

- [x] Update knowledge-routing.md — remove deleted tool rows (claim_link, claim_conflicts, claim_reconcile, claim_search, claim_dedup, claim_discover, claim_embed, research_summarize, research_review), keep knowledge_search, knowledge_embed, knowledge_conflicts, wiki_write, wiki_search, wiki_read, research_fetch | scope: container/skills/wiki-manager/knowledge-routing.md | size: S
- [x] Update wiki-manager instructions.md — remove claim tool usage examples, simplify article lifecycle, remove NLI verification references | scope: container/skills/wiki-manager/instructions.md | size: S
- [x] Update wiki-manager SKILL.md — remove claim provenance sections, update tool list | scope: container/skills/wiki-manager/SKILL.md | size: S
- [x] Update knowledge-wiki skill files — article-conventions.md and content-model.md, remove claims references, simplify episodic tier | scope: ~/.claude/skills/knowledge-wiki/ | size: S
- [x] Codify inline citation convention — sources frontmatter + inline [source-slug] citations | scope: container/skills/wiki-manager/instructions.md | size: S
- [x] Validation sweep — zero grep matches for deleted tool names across all skill files | scope: container/skills/**/*, ~/.claude/skills/**/* | size: S

</details>

## Phase 44: Curated Article Embedding + Index-in-Context

- [x] Add type "curated" to knowledge store type system + search filter: test searchSimilar filters by type 'curated' and KnowledgeInsert accepts type 'curated' (RED), add 'curated' to type union in KnowledgeInsert and to validType check in handleKnowledgeSearch (line 124 currently hardcodes claim|sentence|insight), update MCP tool description string to include "curated" (GREEN) | scope: container/agent-runner/src/mcp-tools/knowledge-vector-store.ts, container/agent-runner/src/mcp-tools/knowledge-tools.ts | success: `cd container/agent-runner && bun run typecheck && grep -q "'curated'" src/mcp-tools/knowledge-vector-store.ts && grep -q "'curated'" src/mcp-tools/knowledge-tools.ts` | size: S
- [x] Citation marker stripping utility for curated articles: test stripCitationMarkers removes [slug-with-hyphens] patterns but preserves [1], [note], and regular text (RED), implement stripCitationMarkers(text: string): string in sentence-embed-pipeline.ts using regex /\[[a-z][a-z0-9]*(?:-[a-z0-9]+)+\]/g that requires at least one hyphen, export for use in curated embedding path (GREEN) | scope: container/agent-runner/src/mcp-tools/sentence-embed-pipeline.ts, container/agent-runner/src/mcp-tools/sentence-embed-pipeline.test.ts | success: `cd container/agent-runner && bun test src/mcp-tools/sentence-embed-pipeline.test.ts` | size: S
- [x] Add curated article embedding path to knowledge_embed: test handleKnowledgeEmbed with source="articles" recursively reads .md files from articles/ subdirectories and produces entries with type='curated' in knowledge.db, test that citation markers are stripped before embedding, test that state tracking uses "articles:" prefix for curated files (RED), add source "articles" branch in handleKnowledgeEmbed that sets dir to path.join(wikiPath, 'articles') with recursive .md file listing, call stripCitationMarkers on content before passing to embedSentences, add typeOverride param to embedSentences that when set bypasses claim/insight classification entirely and uses the override type for all entries, key processedArticles with "articles:" prefix for curated source (GREEN), extract recursive .md file listing into listMdFilesRecursive helper (REFACTOR) | scope: container/agent-runner/src/mcp-tools/knowledge-tools.ts, container/agent-runner/src/mcp-tools/sentence-embed-pipeline.ts, container/agent-runner/src/mcp-tools/knowledge-tools.test.ts | success: `cd container/agent-runner && bun test src/mcp-tools/knowledge-tools.test.ts` | size: M
- [x] Extend wiki-bridge for curated article index at spawn: test generateWikiContext output includes article slugs and titles grouped by category for each wiki, test article index section appears after existing domain map, test combined output stays under 10000 chars per wiki for 150 articles (RED), add buildArticleIndex(wikiPath) that recursively reads articles/ .md files extracting title from frontmatter and category from subdirectory name, render as compact one-line-per-article format grouped by category, integrate into renderWikiSection after existing topics line (GREEN), add warning log if article count exceeds 500 (REFACTOR) | scope: src/modules/memory/wiki-bridge.ts, src/modules/memory/wiki-bridge.test.ts | success: `pnpm vitest run src/modules/memory/wiki-bridge.test.ts` | size: M
- [x] Update documentation — knowledge-routing, wiki-manager, MCP descriptions: verify docs currently reference only claim/sentence/insight types (RED), add curated type to knowledge-routing.md intent-to-tool table with usage guidance, update wiki-manager instructions.md with curated embedding guidance and index-in-context explanation, update knowledge_embed MCP tool description to mention source="articles" for curated embedding (GREEN) | scope: container/skills/wiki-manager/knowledge-routing.md, container/skills/wiki-manager/instructions.md, container/agent-runner/src/mcp-tools/knowledge-tools.ts | success: `grep -q 'curated' container/skills/wiki-manager/knowledge-routing.md && grep -q 'articles' container/skills/wiki-manager/instructions.md && grep -q 'articles' container/agent-runner/src/mcp-tools/knowledge-tools.ts` | size: S
- [x] Build verification + regression check: run full container test suite + host test suite + both typechecks, verify no regressions (RED/GREEN) | scope: container/agent-runner/src/**, src/** | success: `cd container/agent-runner && bun test 2>&1 | grep -E '0 fail' && bun run typecheck && cd ../.. && pnpm run build && pnpm vitest run` | size: S
